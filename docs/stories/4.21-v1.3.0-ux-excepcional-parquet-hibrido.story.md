# Story 4.21 — UX excepcional + parquet híbrido (v1.3.0)

**Status:** Done
**Epic:** 4 (UX & release hardening)
**Created:** 2026-05-13 (BIG COUNCIL round 4 — Pichau directive)
**Owner:** @aiox-master (Orion) — autonomous mode; impl @dev (Dex) / @ux-design-expert (Uma) / @data-engineer (Dara) / @architect (Aria); perf @perf (Pyro); QA @qa (Quinn)

---

## Contexto / Problema

Pichau (mensagem direta 2026-05-13, pós-validação v1.2.0):
> "está muito bom, 2 pontos: 1. ProgressCard polish; 2. catálogo vazio (Bug 2 — crítico); 3. DLL status durante download; 4. cancelar não para; 5. ícone do projeto; 6. atalho desktop pelo instalador; 7. SmartScreen warning. Peça para o squad propor melhorias para todo projeto e produto, inclusive chame o responsável pela otimização da velocidade do download, vamos fazer o possível para a nova v ser excepcional."

Segunda directive (2026-05-13, pre-implementation alignment):
> "precisamos de parquets mensais, quando baixarmos datas maiores que mês, parquets mensais. quando baixarmos datas menores, vamos ter parquets de dias, se não é muito ruim, vai ficar muito parquet, e pra qlqr backtest fica ruim"
>
> "Sim, mas isso de híbrido tem que funcionar, vamos rigidamente implementar e testar"

Council de 6 agentes (mini-councils paralelos: Aria/architect, Uma/ux, Dara/data-eng, Pyro/perf, Quinn/qa, Felix/devops). Plano consolidado em `docs/qa/V1.3.0-PLAN.md` (4 waves).

## Acceptance Criteria

- [x] **AC1 — Layout parquet híbrido (ADR-025):** `{ex}/{sym}/{YYYY}/{MM}.parquet` quando mês completo, `{ex}/{sym}/{YYYY}/{MM}/{DD}.parquet` durante o mês corrente / downloads parciais. `is_month_complete()` checa `chunk_ledger` vs calendário B3 (todos os dias úteis com status `completed` ou `no_trades`).
- [x] **AC2 — Auto-compact atômico:** `maybe_compact_month()` ao final de cada `register_partition` — escreve `{MM}.parquet` (tmp + `os.replace` + fsync + SHA256 verify), só **DEPOIS** deleta `{DD}.parquet` daquele mês. Crash mid-compact recupera na boot via tabela `compactions` (completa se íntegro / reverte se inconsistente). Migração `CATALOG_VERSION` 1.2.0→1.3.0 idempotente.
- [x] **AC3 — Validação rígida:** testes T1-T9 (integração) cobrindo: mês completo→compacta / incompleto→não compacta / crash recovery completa / crash recovery reverte / write-once / layout exclusivo / idempotência / double-register / race condition. **Property test (Hypothesis, 50 seeds)** com 3 invariantes: conservação (`sum(MM)==sum(DD originais)`), exclusividade mútua (nunca `{MM}.parquet` e `{MM}/{DD}.parquet` coexistem), consistência catálogo↔FS. T10 (smoke real em pregão B3) deferido para Pichau pós-build.
- [x] **AC4 — Bug 2 (catálogo vazio após download via UI):** nova `bundle_paths.default_data_dir()` consolidando os 3 callsites (DownloadScreen, CatalogScreen, SettingsScreen). RCA: `CatalogScreen` lia de `Path.cwd()/data` (= `System32\data\` quando o app era lançado pelo atalho do Setup); `DownloadScreen` gravava em `user_data_dir()/data`.
- [x] **AC5 — Bug 3 (DLL status statusbar durante download):** observer pattern puro Python em `dll/session.py` (5 estados: idle/connecting/connected/downloading/reconnecting/error) + adapter Qt singleton `DllSessionAdapter` marshalando via `QueuedConnection`. Statusbar reflete estado em tempo real.
- [x] **AC6 — Bug 4 (cancelar não para):** microcopy explícita "Cancelando — aguardando dia atual terminar (até ~60s)" + botão CANCELAR 40px com ícone ⏹ + hint contextual. Cancelamento mid-chunk **não** foi implementado intencionalmente (DLL não tem API de cancel; matar mid-trade corromperia o buffer interno).
- [x] **AC7 — Bug 5 (ícone do projeto):** SVG mestre `installer/assets/data_downloader.svg` (candle cyan + seta de download) + `scripts/build-icon.py` gera `.ico` multi-resolução (16/32/48/64/128/256, 13.5KB Pillow puro — sem cairosvg). `MainWindow.setWindowIcon` + InnoSetup `SetupIconFile` + PyInstaller spec `icon=`.
- [x] **AC8 — Bug 6 (atalho desktop pelo Setup):** `{commondesktop}` → `{autodesktop}` (read-only em non-admin causava falha silenciosa). + `IconFilename` no `[Icons]`.
- [-] **AC9 — Bug 7 (SmartScreen / code signing):** **DEFERIDO para v1.4.0** (sem orçamento agora). Pichau approved via AskUserQuestion.
- [x] **AC10 — UX excepcional:** onboarding wizard de 3 telas no 1º run; StorageIndicator no statusbar (verde/amarelo/vermelho ≥20/5-20/<5 GB); ProgressCard polish (hierarquia tipográfica, ícones unicode, cores dinâmicas, _SegmentedProgressBar, divisores); CTA "Ver no catálogo" ao concluir.
- [x] **AC11 — Performance (@perf Pyro):** `_ROW_GROUP_SIZE` 100k→1M (+30% throughput); `gc.freeze()` entre chunks (7-12% menos tempo total em downloads 30+d); write-once quando `partition.day` (sem read-merge-rewrite O(N²) — 700ms→50ms por dia WDOFUT).
- [x] **AC12 — Suite verde:** `ruff` 0 / `mypy --strict` 0 (90 source files) / `pytest` 1222 unit + 494 integration + 65 property, 0 fail (4 skips ambientais).

## File List

- **Wave 1 (gates + version):** `pyproject.toml` (1.2.0→1.3.0), `src/data_downloader/__init__.py` (_PACKAGE_VERSION), `installer/data_downloader.iss` (AppVersion)
- **Wave 2A (DLL session observer + adapter):** `src/data_downloader/dll/session.py` (NEW — observer puro Python, 5 estados, register/unregister/set_state thread-safe), `src/data_downloader/ui/adapters/dll_session_adapter.py` (NEW — Qt singleton + QueuedConnection), `src/data_downloader/ui/main_window.py` (statusbar widget + connect)
- **Wave 2B (Catalog adapter + CTA "Ver no catálogo"):** `src/data_downloader/storage/catalog.py` (observer `partition_registered`, debounce-aware), `src/data_downloader/ui/adapters/catalog_adapter.py` (`partition_registered = Signal(str, int, int)` com debounce 500ms via QTimer), `src/data_downloader/ui/widgets/progress_card.py` (`view_catalog_requested = Signal(str)` + handler na MainWindow)
- **Wave 2C (ProgressCard polish):** `src/data_downloader/ui/widgets/progress_card.py` (_title_label 20px/700, `_SegmentedProgressBar` custom paintEvent, ícones unicode ⏱⏳⚡✓⚠↻, cores semânticas dinâmicas, divisores 1px, botão CANCELAR 40px + ⏹), `src/data_downloader/ui/microcopy_loader.py` (BTN_CANCELLING, LBL_PROGRESS_TITLE_DOWNLOADING, INF_CALCULATING_PLAN, LBL_CANCELLING_HINT)
- **Wave 2D (Bug 2 + ícone):** `src/data_downloader/_internal/bundle_paths.py` (`default_data_dir()` NEW), `src/data_downloader/ui/main_window.py` (catalog_screen recebe `data_dir=default_data_dir()` + `setWindowIcon` via `bundle_paths.asset_path`), `src/data_downloader/ui/screens/settings_screen.py` (fallback `default_data_dir()`), `installer/assets/data_downloader.svg` (NEW), `installer/assets/data_downloader.ico` (NEW — multi-res), `src/data_downloader/ui/assets/icon.ico` (NEW — mirror), `scripts/build-icon.py` (NEW — Pillow puro), `installer/data_downloader.iss` (SetupIconFile + IconFilename + `{commondesktop}`→`{autodesktop}` Bug 6), `build/data_downloader.spec.template` (icon=)
- **Wave 3 (parquet híbrido + auto-compact + ADR-025 — RIGOROSO):** `docs/adr/ADR-025-parquet-per-day-hybrid.md` (NEW), `src/data_downloader/storage/partition.py` (`PartitionKey.day: int | None`, `resolve_partition_path` 2 layouts, `parse_partition_path` 4 ou 5 segments), `src/data_downloader/storage/parquet_writer.py` (`_ROW_GROUP_SIZE=1_000_000`, write-once quando day, `compact_month()` NEW — atomic tmp+replace+fsync+SHA256), `src/data_downloader/storage/schema.py` (CATALOG_VERSION 1.3.0, deltas idempotentes: ADD COLUMN partitions.day, idx_partitions_symbol_ymd, table compactions, idx_compactions_inflight), `src/data_downloader/storage/catalog.py` (`is_month_complete`, `maybe_compact_month`, `_recover_inflight_compactions`, `_resolve_inflight_compaction`), `src/data_downloader/orchestrator/orchestrator.py` (gc.freeze + maybe_compact_month hook + state_monitor + pause_event), testes: `tests/integration/test_parquet_per_day_hybrid.py` (T1-T9), `tests/property/test_parquet_per_day_hybrid_property.py` (50 seeds, 3 invariantes), `tests/integration/test_calendar_b3_holidays_dat.py` (fix `REAL_DAT_PATH` para path absoluto, teste-pollution na suite full)
- **Wave 4A (onboarding wizard):** `src/data_downloader/ui/screens/onboarding_wizard.py` (NEW — QDialog 3 telas + `is_onboarding_needed()` helper), `src/data_downloader/ui/app.py` (trigger entre `bootstrap_env` e `MainWindow()`), `src/data_downloader/ui/microcopy_loader.py` (LBL_ONBOARDING_* — 18 IDs), tests: `tests/integration/test_onboarding_wizard.py` (16 tests)
- **Wave 4B (StorageIndicator):** `src/data_downloader/ui/widgets/storage_indicator.py` (NEW — QLabel + QTimer 30s, format pt-BR, cores verde/amarelo/vermelho), `src/data_downloader/ui/main_window.py` (statusbar widget), `src/data_downloader/ui/microcopy_loader.py` (LBL_STORAGE_INDICATOR, TIP_STORAGE_INDICATOR, WAR_STORAGE_LOW), tests: `tests/integration/test_storage_indicator.py` (23 tests)
- **Release:** `pyproject.toml`/`__init__.py`/`installer/data_downloader.iss` (1.2.0→1.3.0), `CHANGELOG.md`, `README.md`, `docs/release-notes/v1.3.0.md`, `docs/qa/V1.3.0-PLAN.md`

## QA Results

- **Verdict: PASS** (automatizável). ruff 0 / mypy 0 / pytest **1222 unit + 494 integration + 65 property, 0 fail** (4 skips ambientais).
- **T10 (smoke real em pregão B3 com auto-compact ao fechar mês):** **PENDENTE — Pichau pós-build**. Validar: download via UI de 1 mês WDOFUT em pregão (auto-compact dispara ao fechar mês + completeness ~100% + layout híbrido no FS).

## Deferido para v1.4.0+

- **Code signing (Bug 7 / SmartScreen)** — sem orçamento agora; Pichau approved deferimento via AskUserQuestion. Workaround documentado em `docs/release/INSTALL.md`.
- **Pause/Resume mid-download na UI** (suspend/resume no orchestrator + botão).
- **Reconnect-without-reinit** — state_monitor v1.3.0 já detecta queda e seta `pause_event`; falta handshake de reconexão sem `Finalize` (Q08-E aplicável).
- **CatalogScreen heatmap de cobertura** + filtro por ano (UX para 7 anos × N símbolos).

## Change Log

- 2026-05-13 — Story criada (BIG COUNCIL round 4, modo autônomo). Wave 1+2+3+4 implementadas e validadas. Status: Done. Commit: [TBD].
