---
title: "1.6 — Contract calendar (mapa de contratos vigentes WDO)"
epic: 1
story: 6
status: Ready
owner: dev
reviewers: [storage-engineer, profitdll-specialist, qa]
priority: P0
estimate: 1d
depends_on: [1.2, 1.3, 1.5]
---

# Story 1.6 — Contract calendar (WDO map vigente)

## User Story

**As a** orchestrator (Story 1.7) ou usuário CLI
**I want** uma função `vigent_contract(symbol_root, on_date)` que resolve "WDO" + data → "WDOJ26" (ou erro se não houver vigente), populada inicialmente para WDO 2025-2026 e validada via probe na DLL
**So that** Story 1.7 baixe contrato vigente correto sem chutar letras de mês (lei R9), e usuário não use `WDOFUT` (que retorna 0 trades — quirk Nelo).

---

## Acceptance Criteria

1. **AC1:** Sol cria `docs/storage/CONTRACTS.md` documentando: tabela de letras de mês CME/B3, regras de vigência por símbolo (WDO mensal, WIN trimestral H/M/U/Z), e mapa inicial de contratos validados.
2. **AC2:** `src/data_downloader/orchestrator/contracts.py` expõe função `vigent_contract(symbol_root: str, on_date: date, *, exchange: str = 'F') -> str`.
3. **AC3:** Função consulta tabela `contracts` no SQLite catalog (Story 1.5) — não calcula, faz lookup.
4. **AC4:** Levanta `InvalidContract(symbol_root, on_date)` se não houver contrato vigente.
5. **AC5:** Função auxiliar `populate_contracts_from_seed(catalog: Catalog, seed_path: Path) -> int` carrega tabela inicial de `docs/storage/CONTRACTS.md` (formato YAML embutido OU tabela Markdown parseável).
6. **AC6:** CLI command `data-downloader contracts validate {symbol_root} {contract_code}` faz probe via DLL (Story 1.2 init) chamando `GetHistoryTrades` em primeiro dia vigente; valida que retorna trades > 0 (delegação a Nelo `*probe-dll` via código).
7. **AC7:** `validate` atualiza tabela `contracts` com `validated_at = now()`, `validation_source = 'dll_probe'`.
8. **AC8:** Mapa inicial popula no mínimo: `WDOJ26` (vigente abr/26), `WDOK26` (mai/26), `WDOH26` (mar/26 — referência do passado próximo). Datas de vigência: hipótese inicial = penúltimo dia útil mês X-1 até penúltimo dia útil mês X (validar com Sol + Nelo via probe).
9. **AC9:** Property test: para todo `(root, date)` em range testado, `vigent_contract(root, date)` retorna contract com `vigent_from <= date <= vigent_until`.
10. **AC10:** Test integration: smoke test (gated por env) chama `validate WDO WDOJ26` e confirma probe retorna trades.

## Tasks / Subtasks

- [ ] **Task 1 — Sol cria CONTRACTS.md**
  - [ ] Subtask 1.1: Sol `*contracts-doc` produz `docs/storage/CONTRACTS.md` com:
    - Tabela letras de mês (F-Z mapping)
    - Regras de vigência hipotetizadas (WDO, WIN)
    - Mapa inicial validado (≥3 contratos WDO recentes)
    - Formato seed (YAML embedded ou Markdown table parseável)
- [ ] **Task 2 — Implementar `orchestrator/contracts.py`**
  - [ ] Subtask 2.1: Função `vigent_contract(root, on_date, exchange) -> str`
  - [ ] Subtask 2.2: Exception `InvalidContract`
  - [ ] Subtask 2.3: Função `month_letter(month: int) -> str` (F=1..Z=12) com property test
  - [ ] Subtask 2.4: Função `month_from_letter(letter: str) -> int` (inverso)
  - [ ] Subtask 2.5: Função `populate_contracts_from_seed(catalog, seed_path) -> int`
- [ ] **Task 3 — Implementar `orchestrator/contracts_probe.py`**
  - [ ] Subtask 3.1: Função `probe_contract(dll, root, contract_code, sample_date) -> ProbeResult`
  - [ ] Subtask 3.2: Usa primitive de Story 1.3 `download_chunk` para 1 dia
  - [ ] Subtask 3.3: Sucesso = `trades > 0`; falha = log + return ProbeResult(success=False, reason)
- [ ] **Task 4 — CLI commands em `cli.py`**
  - [ ] Subtask 4.1: `contracts list [--root WDO]` lista contratos cadastrados
  - [ ] Subtask 4.2: `contracts add {root} {month_letter} {year}` adiciona ao catálogo
  - [ ] Subtask 4.3: `contracts validate {root} {contract_code}` faz probe DLL
  - [ ] Subtask 4.4: `contracts vigent {root} {date}` retorna contract code
- [ ] **Task 5 — Seed inicial em CONTRACTS.md**
  - [ ] Subtask 5.1: Adicionar (Sol valida): WDOH26 (mar/26), WDOJ26 (abr/26), WDOK26 (mai/26)
  - [ ] Subtask 5.2: Vigência conforme regra hipotetizada — Sol marca `validated_at = NULL` até probe
- [ ] **Task 6 — Testes**
  - [ ] Subtask 6.1: `tests/unit/test_month_letter.py` — F=1...Z=12, inverso
  - [ ] Subtask 6.2: `tests/unit/test_vigent_contract.py` — lookup correto, InvalidContract
  - [ ] Subtask 6.3: `tests/unit/test_seed_loader.py` — populate seed funciona, idempotente
  - [ ] Subtask 6.4: `tests/property/test_vigent_invariant.py` — Hypothesis: vigent retornado tem from<=date<=until
  - [ ] Subtask 6.5: `tests/smoke/test_probe.py` — gated env, valida WDOJ26 real
- [ ] **Task 7 — Auditoria**
  - [ ] Subtask 7.1: Sol `*audit-storage-pr` (mudou catálogo)
  - [ ] Subtask 7.2: Nelo confirma quirk WDOFUT vs vigente em comentário do código (`*history-api --asset WDO`)

## Dev Notes

### Referências
- ADR-006 (calendário tabela versionada)
- `docs/storage/CONTRACTS.md` (Sol cria)
- `agents/storage-engineer.md` `expertise.contract_calendar_seed`
- `agents/profitdll-specialist.md` core_principles (quirk WDOFUT)
- Lei R8 (bolsa = letra), R9 (não chutar contratos)

### Constraints CRÍTICAS
- **Não chutar:** mesmo que pareça óbvio que `WDOJ26` = abril 2026, validar via probe.
- **Lookup, não cálculo:** função `vigent_contract` consulta SQLite — não roda algoritmo. Algoritmo está implícito na seed inserida.
- **Probe usa Story 1.3:** depende de DLL inicializada. Smoke test gated por env.

### Formato seed proposto (em CONTRACTS.md)

```yaml
# docs/storage/CONTRACTS.md (Sol formaliza)
contracts:
  - root: WDO
    code: WDOH26
    month_letter: H
    year: 2026
    vigent_from: 2026-02-26
    vigent_until: 2026-03-30
    validated_at: null  # preencher após probe
    validation_source: hypothesized
  - root: WDO
    code: WDOJ26
    month_letter: J
    year: 2026
    vigent_from: 2026-03-31
    vigent_until: 2026-04-29
    validated_at: null
    validation_source: hypothesized
  # ...
```

### Decisão pendente (consultar Sol + Nelo)
- Regra exata de vigência WDO — "penúltimo dia útil mês X-1" é hipótese. Sol valida com tabela oficial Nelogica/B3. Se diverge, atualiza seed.

## Testing

- **Unit:** month_letter, vigent_contract lookup, seed loader
- **Integration:** populate + read back
- **Property-based:** vigent invariant
- **Smoke:** probe real (gated)

## File List

(preencher durante implementação)

## Dev Agent Record

- **Agent Model Used:**
- **Debug Log:**
- **Completion Notes:**
- **Change Log:**
