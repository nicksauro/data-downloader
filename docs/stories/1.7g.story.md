---
title: "1.7g — P0 release-blockers — schema integrity + volume completude"
epic: 1
story: 7.7
status: InReview
owner: dev
reviewers: [qa, architect, profitdll-specialist, po]
priority: P0
estimate: 1d
depends_on: [1.7d]
parent: docs/stories/1.7d.story.md
related:
  - docs/decisions/COUNCIL-36-Pax-release-blockers-2026-05-05.md
  - docs/decisions/COUNCIL-32-Nelo-agents-trade-types-2026-05-05.md
  - docs/decisions/COUNCIL-35-Sol-documentacao-2026-05-05.md
  - docs/qa/SMOKE_EVIDENCE/1.7d-20260505T160339Z-postfix35-VALIDATED-PERSISTED-PASS.md
  - docs/stories/1.8.story.md
---

# Story 1.7g — P0 release-blockers — schema integrity + volume completude

## Contexto

Após COUNCIL-36 (Pax/@po), dois bloqueadores P0 inegociáveis foram
identificados que **impedem o release v1**:

### Bloqueador 1 — Silent column drop é BUG, não trade-off

COUNCIL-32 (Nelo) descobriu que o schema parquet v1.0.0
(`src/data_downloader/storage/schema.py`) **descarta silenciosamente**
três colunas que o resolver popula corretamente em runtime:

- `buy_agent_name` (ex: "XP Investimentos") — populado por `AgentResolver.resolve()`
- `sell_agent_name` (ex: "BTG Pactual") — idem
- `trade_type_name` (ex: "AgressionBuy") — derivável do enum `TTradeType`

O comentário em `download_primitive.py` L153-155 admite explicitamente:
> "Campos NÃO fazem parte do schema Parquet v1.0.0 (Sol authority — adição requer bump SCHEMA_VERSION); writer atual descarta se não encontrar coluna."

**Decisão Pax (autoridade ownership):** descarte silencioso de colunas é
**BUG**, não trade-off aceitável. Schema bump v1.0.0 → v1.1.0 é
release-blocker. Adicionalmente, o writer deve **falhar loudly** ao
receber colunas não mapeadas (defense-in-depth contra futuras regressões
deste tipo).

### Bloqueador 2 — Volume capturado parece ~50% abaixo do esperado

Smoke real 1.7d capturou 603.770 trades em 4 dias calendário (= 2 dias
úteis: seg 04/05 + ter 05/05 parcial; 01/05 = feriado, 02-03/05 = fds).

Dia útil completo (04/05, 9h25m de pregão) → 307.010 trades.

Usuário (autoridade ownership) afirma que **1 dia útil WDOFUT = 600-700k
trades**. Pro-rated full day no smoke = ~326k. **Gap ≈ 50%**.

Isso implica perda silenciosa em algum ponto da cadeia
`subscribe_ticker → callback V2 → IngestorThread → TranslateTrade → result.trades → parquet`.

Hipóteses ranqueadas (COUNCIL-36 §1):
- H1 (30%) — `subscribe_ticker` em ordem ruim vs `GetHistoryTrades`
- H2 (30%) — LAST_PACKET prematuro (DLL declara fim antes do tempo)
- H3 (20%) — TranslateTrade descartando trades reais como sentinelas Q-DRIFT-34
- H4 (15%) — janela 5 dias é cap silencioso da DLL
- H5 (5%) — buffer overflow callback V2 em alta liquidez

**Decisão Pax:** investigação volume é P0. Não shipa v1 sem confirmar
captura ≥500k trades em 1 dia útil completo de WDOFUT.

### Bloqueadores complementares (P0 docs)

- **SCHEMA.md L33** (Sol) — comentário trade_type errado: dizia
  "1=auction, 2=normal, 3=cross"; correto é `1=CrossTrade`,
  `2=AgressionBuy`, `3=AgressionSell`, `4=Auction` (Nelo COUNCIL-32 §3.1).
  Tabela completa do enum `TTradeType` (14 valores) deve ser adicionada.
- **wrapper.py L731-745** — comentário Q-DRIFT-35 errado: dizia
  "`-2147483636 == 0x80000004 reinterpretado`"; correto é
  `-2147483636 == NL_NOT_FOUND == 0x8000000C` (semântica intencional da
  DLL, não corrupção ABI — Nelo COUNCIL-32 §2.2).

## User Story

**As a** dono do produto data-downloader (Pax via Pichau)
**I want** garantir que o parquet entregue ao analista contém TODAS as
colunas resolvíveis pela DLL (sem descartes silenciosos) E que o volume
de trades capturados reflete a realidade do mercado (≥500k/dia útil
para WDOFUT)
**So that** análises downstream (microestrutura, agressões por corretora,
volume por tipo de trade) sejam confiáveis e o release v1 possa shipar
com integridade de dados garantida.

## Acceptance Criteria

1. **AC1 — Schema parquet v1.1.0 com colunas de nomes (NOT NULL com fallback string)**.
   Em `src/data_downloader/storage/schema.py`:
   - `SCHEMA_VERSION = "1.1.0"` (bump de "1.0.0").
   - Adicionar 3 campos `pa.field(..., pa.string(), nullable=False)`:
     - `buy_agent_name`
     - `sell_agent_name`
     - `trade_type_name`
   - Fallbacks determinísticos quando resolver retorna None ou ID
     desconhecido:
     - `buy_agent_name` / `sell_agent_name`: `f"Agent#{id}"` (mesmo
       padrão do `AgentResolver.resolve()` para NL_NOT_FOUND).
     - `trade_type_name`: lookup do enum `TTradeType` (14 valores), com
       fallback `f"TradeType#{n}"` se valor estiver fora do range 0-13.
   - Atualizar `validate_record()` (INT-3/4/5) para aceitar/exigir os
     novos campos.

2. **AC2 — Writer FALHA LOUDLY se schema receber colunas não mapeadas (no silent drop)**.
   Em `src/data_downloader/storage/writer.py` (ou módulo equivalente):
   - Antes de gravar batch, validar `set(record_keys) ⊆ set(schema_columns)`.
   - Se houver chave em `record_keys` que não esteja em `schema_columns`,
     **raise `SchemaIntegrityError`** (nova exception class) com mensagem
     clara listando as colunas órfãs.
   - **NÃO ignorar silenciosamente.** O comportamento atual ("writer
     descarta se não encontrar coluna") é o BUG que esta AC corrige.
   - Adicionar teste unit que valida o raise: passar dict com chave
     `unknown_field=foo` deve quebrar com `SchemaIntegrityError`.

3. **AC3 — Smoke real WDOFUT 1 dia útil retorna ≥500k trades (validação volume)**.
   Quinn (@qa) executa smoke real standalone (mesmo padrão 1.7d) com:
   - Símbolo: WDOFUT/F
   - Janela: 1 dia útil completo (`now - 1 dia útil 00:00 BRT` → `now - 1 dia útil 23:59 BRT`).
   - Exemplo: se hoje (terça) for útil, janela = segunda 00:00 → segunda 23:59 BRT.
   - **Critério PASS**: `trades_count >= 500_000`.
   - **Critério FAIL**: `trades_count < 500_000` → escalar para
     mini-council Aria + Nelo + Pax (não tentar resolver autonomamente).
   - Persistir parquet ad-hoc em `data/scratch/smoke-1.7g-{uuid}/wdofut.parquet`
     para análise DuckDB.

4. **AC4 — Distribuição diária no parquet ≥500k para cada dia útil dentro da janela**.
   Smoke adicional com janela 5 dias (mesmo padrão 1.7d) — para CADA dia
   útil presente no parquet:
   - `SELECT date(timestamp_ns/1e9) AS d, count(*) AS n FROM wdofut.parquet GROUP BY d ORDER BY d`
   - Cada dia útil completo deve ter `n >= 500_000`.
   - Dias parciais (primeiro/último da janela) ignorados — apenas dias
     com `min(timestamp_ns)` antes de 09:30 BRT e `max(timestamp_ns)`
     depois de 17:30 BRT contam como "completos".
   - Se algum dia útil completo tiver `n < 500_000` → root-cause volume
     gap (H1/H2/H3/H4/H5) precisa ser identificado e fixado nesta story
     ou escalado para mini-council.

5. **AC5 — SCHEMA.md tabela TTradeType de 14 valores**.
   Em `docs/SCHEMA.md` L33 (e onde mais aparecer):
   - Substituir comentário errado ("1=auction, 2=normal, 3=cross") pela
     tabela canônica do manual `LegacyProfitDataTypesU.pas` L33-46:

     | ID | Nome | Notas |
     |----|------|-------|
     | 0 | ttZero | reserved/sentinel |
     | 1 | ttCrossTrade | cross trade |
     | 2 | ttAgressionBuy | comprador agride |
     | 3 | ttAgressionSell | vendedor agride |
     | 4 | ttAuction | leilão |
     | 5 | ttSurveillance | vigilância |
     | 6 | ttExpit | expit |
     | 7 | ttOptionsExercise | exercício de opções |
     | 8 | ttOverTheCounter | OTC |
     | 9 | ttDerivativeTerm | termo derivativos |
     | 10 | ttIndex | índice |
     | 11 | ttBTC | BTC |
     | 12 | ttOnBehalf | on behalf |
     | 13 | ttRLP | retail liquidity provider |

   - Adicionar referência cruzada: `Veja src/data_downloader/storage/schema.py::TRADE_TYPE_NAMES (mapping canônico)`.

7. **AC7 — Queue resilience anti-overflow (root-cause AC4)**.
   Após smoke 4-day em COUNCIL-37 (Quinn / H-E) confirmar gap 71% por
   `queue.Full` silencioso em `dll/callbacks.py::_history_cb`:
   - **A7.1** — `src/data_downloader/orchestrator/download_primitive.py:88`:
     `TRADE_QUEUE_MAXSIZE: Final[int] = 2_000_000` (bump de 100_000).
     ~32 MB RAM par-de-int64 — folga p/ 5 dias úteis WDOFUT a 7-10k
     trades/s sustentado em burst.
   - **A7.2** — `src/data_downloader/dll/callbacks.py::make_history_trade_callback_v2`:
     accept `stats: dict[str, int] | None = None`. Em `queue.Full`,
     `stats["queue_dropped"] += 1` (single-bytecode GIL-atômico —
     respeita R3, não bloqueia ConnectorThread). `stats=None` ⇒ legacy.
   - **A7.3** — `download_primitive.download_chunk`: inicializar
     `callback_stats = {"queue_dropped": 0}`, passar para factory, ler
     após `join()` das threads.
   - **A7.4** — `ChunkResult.queue_dropped: int = 0` campo novo
     (frozen dataclass, default = backward-compat).
   - **A7.5** — Log `download.complete` inclui
     `queue_dropped=callback_stats["queue_dropped"]`.
   - **A7.6** — Test unit: callback com `stats={"queue_dropped":0}` em
     `Queue(maxsize=2)`, fire 4 trades → `queue_dropped == 2` e qsize == 2.
   - **A7.7** — Test unit backward-compat: callback com `stats=None`
     mantém engulir silencioso (Story 1.3 INV-1/R3).

8. **AC8 — Gate contingente queue_dropped > 0 (concessão @architect)**.
   Quinn executa smoke 5-dia pós-fix (AC7). Avaliar `result.queue_dropped`:
   - **`queue_dropped == 0`**: AC7 sucesso → AC4 destravada com smoke
     re-validado por dia útil. 1.7g pronta para QA gate.
   - **`queue_dropped > 0`**: 2M ainda saturou em burst. Bump foi
     insuficiente. Honra ADR-020 Nível 4: spin-off **Story 1.7h —
     chunking automático** (sub-divisão `download_chunk` em janelas ≤1d
     quando intervalo solicitado > 1d). 1.7g fecha com AC7 implementado
     mas AC4 BLOCKED-by-1.7h; ship v1 só após 1.7h.

6. **AC6 — Comentário Q-DRIFT-35 wrapper.py corrigido (NL_NOT_FOUND)**.
   Em `src/data_downloader/dll/wrapper.py` L731-745 (ou onde estiver o
   comentário Q-DRIFT-35):
   - Substituir `-2147483636 (== 0x80000004 reinterpretado como c_int signed)` por
     `-2147483636 (== NL_NOT_FOUND == 0x8000000C — código semântico da DLL para "agent_id desconhecido", NÃO corrupção ABI)`.
   - Manter o fix de signatures (`argtypes`/`restype` explícitos para
     `GetAgentNameLength`/`GetAgentName` em path `minimal_handshake`) —
     continua sendo boa prática ctypes em x64. Apenas corrigir o
     **diagnóstico textual** do comentário.
   - Adicionar nota: `Veja COUNCIL-32 §2.2 e error_taxonomy.py::_NL_NOT_FOUND`.

## Out of scope (NÃO fazer nesta story)

- Telemetria `translate_failures` em 3 buckets (sentinel/nl_error/exception)
  — diferida para 1.7h ou 1.8 (Nelo COUNCIL-32 §4).
- Filtro adicional de preço/quantidade fora de range plausível
  (validate_record extras) — diferido (anomalias 0.039%, não bloqueia).
- Consolidação ampla de Q-DRIFT-13..30 em QUIRKS.md — diferida para 1.7f
  (Sol COUNCIL-35 §6).
- Story 1.8 original (schema bump) — **absorvida** por esta story (1.7g).
- Sintoma A pytest harness — segue em WAIVER 1.7b (separado).

## Risks

| # | Risco | Mitigação |
|---|-------|-----------|
| R1 | AC1 quebra readers existentes do parquet v1.0.0 | Migration helper em `storage/schema.py` (read v1.0.0 → cast v1.1.0 com `Agent#{id}` para nomes ausentes). Não bloqueia release v1 do downloader. |
| R2 | AC3 (volume) confirma gap real grande → trabalho de 1.7g vira escopo aberto | Escalar para mini-council Aria + Nelo + Pax; AC3 fail é gate de escalação, não tentar resolver dentro da story. |
| R3 | AC2 (fail-loudly) introduz regressão em pipelines existentes que passam dicts com chaves extras | Audit de todos os call-sites do writer antes do merge; se necessário, normalizar dicts com `dict(filter_keys(...))` no orchestrator (NÃO no writer — writer continua strict). |
| R4 | trade_type_name fallback `TradeType#{n}` não é desejado em produção | Validar com Pax via comment review do PR antes de merge; alternativa é raise se valor fora de 0-13. |

## Dependencies

- 1.7d concluído (smoke standalone validated, base para AC3/AC4)
- COUNCIL-32 (Nelo) — root-cause de AC1, AC5, AC6
- COUNCIL-35 (Sol) — context de docs canonical
- COUNCIL-36 (Pax — este council) — autoridade do bump

## Tasks / Subtasks

- [ ] **T1 (AC1):** schema bump v1.1.0 + 3 colunas + fallbacks
  - [ ] Atualizar `SCHEMA_VERSION` constant
  - [ ] Adicionar fields `buy_agent_name`, `sell_agent_name`, `trade_type_name`
  - [ ] Implementar `TRADE_TYPE_NAMES: dict[int, str]` mapping canônico
  - [ ] Atualizar `validate_record()` para exigir os novos campos
  - [ ] Atualizar `_to_schema_trade()` em orchestrator para preencher os 3 campos
- [ ] **T2 (AC2):** writer fail-loudly
  - [ ] Criar `class SchemaIntegrityError(Exception)`
  - [ ] Adicionar guard no hot path do writer (`set(keys) ⊆ set(schema_cols)`)
  - [ ] Test unit: `unknown_field=foo` raise SchemaIntegrityError
  - [ ] Test integration: dict completo com 3 novos campos passa
- [ ] **T3 (AC3) [Quinn]:** smoke 1 dia útil
  - [ ] Modificar `scripts/run_smoke_real_standalone.py` para janela 1 dia
  - [ ] Executar com `DATA_DOWNLOADER_DLL_MINIMAL_HANDSHAKE=1`
  - [ ] Persistir parquet ad-hoc + log
  - [ ] Validar `trades_count >= 500_000`
  - [ ] Documentar evidência em `docs/qa/SMOKE_EVIDENCE/1.7g-...md`
- [ ] **T4 (AC4) [Quinn]:** smoke 5 dias + volume por dia
  - [ ] Reproduzir 1.7d smoke
  - [ ] Query DuckDB `count(*) GROUP BY date`
  - [ ] Validar cada dia útil completo `>= 500k`
- [ ] **T5 (AC5) [Dex]:** SCHEMA.md tabela TTradeType
- [ ] **T6 (AC6) [Dex]:** wrapper.py comentário Q-DRIFT-35
- [ ] **T7 (AC1+T1) [Nelo, paralelo a Quinn]:** audit `download_chunk` flow
  - [x] Confirmar ordering `subscribe_ticker` vs `GetHistoryTrades` (COUNCIL-38 / 32)
  - [x] Investigar LAST_PACKET timing — root-cause é H-E queue overflow (COUNCIL-37)
  - [x] Cross-check Q-DRIFT-34 guard `wYear<=1900` — sentinelas confirmadas
- [x] **T8 (AC7) [Dex]:** queue resilience implementation
  - [x] Bump `TRADE_QUEUE_MAXSIZE` 100_000 → 2_000_000
  - [x] `make_history_trade_callback_v2(trade_queue, stats=...)` opt-in counter
  - [x] `callback_stats = {"queue_dropped": 0}` em `download_chunk`
  - [x] `ChunkResult.queue_dropped: int = 0` campo novo
  - [x] Log `download.complete` inclui `queue_dropped`
  - [x] 2 testes unit novos (counter increment + backward-compat stats=None)
- [ ] **T9 (AC4 + AC7 validação) [Quinn]:** smoke 5-dia pós-fix
  - [ ] `python scripts/run_smoke_real_standalone.py` com janela 5 dias úteis
  - [ ] Persistir parquet em `data/scratch/smoke-1.7g-{uuid}/wdofut.parquet`
  - [ ] Validar `result.queue_dropped == 0` (AC7) e cada dia útil completo
        ≥500k via DuckDB `count(*) GROUP BY date` (AC4)
  - [ ] Documentar evidência em `docs/qa/SMOKE_EVIDENCE/1.7g-...md`
- [ ] **T10 (AC8 contingente) [Pax + Aria]:** gate decision pós-T9
  - [ ] Se `queue_dropped == 0`: marcar AC7 e AC4 PASS, prosseguir QA gate
  - [ ] Se `queue_dropped > 0`: criar story 1.7h (chunking automático)
        e marcar AC4 BLOCKED-by-1.7h em 1.7g

## Definition of Done

- [x] AC1, AC2, AC5, AC6 satisfeitos (commits 756a61d, 7b1a57f)
- [x] AC3 satisfeito (smoke 1-dia 525.434 trades — log standalone-wdofut-1day-20260505T143122Z)
- [x] AC7 implementado (queue 2M + counter + 2 testes unit novos)
- [ ] AC4 satisfeito (smoke 5-dia pós-fix com cada dia útil ≥500k)
- [ ] AC8 gate avaliado (`queue_dropped == 0` ⇒ PASS; > 0 ⇒ spin-off 1.7h)
- [x] Testes unitários passando (16/16 callbacks + 27/27 download_primitive integration)
- [ ] PR review por @architect (validação de schema migration) + @po (validação de fallbacks)
- [ ] @qa qa-gate verdict = PASS
- [ ] @devops faz push final (git push exclusive)
- [ ] STATUS.md update para v1 release-ready (separado, em story de release)

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-05-05 | Pax (@po, autônomo) | Story criada como release-blocker pós-COUNCIL-36 |
| 2026-05-05 | Orion (@aiox-master) | AC7 + AC8 absorvidos pós-COUNCIL-37 (Quinn / H-E queue overflow). Mini-council Aria/Nelo/Pax 2×1 ABSORVER. Status Draft → InProgress. T8 implementado (queue 2M + queue_dropped counter + 2 testes); T9 (smoke 5-dia) e T10 (gate AC8) pendentes. |
| 2026-05-05 | Gage (@devops, autônomo) | Repo público criado em github.com/nicksauro/data-downloader; push de 9 commits 1.7g+ → origin/main. Status InProgress → InReview. QA gate Quinn PASS já registrado (1 LOW concern AC4 28/04 borderline). Aguarda transition InReview → Done após confirmação humana. |

## Dev Notes

(a ser preenchido por Dex durante implementação)

## File List

Modificados (commits 756a61d, 7b1a57f, e este patch AC7):
- `src/data_downloader/storage/schema.py` (AC1 — SCHEMA_VERSION 1.1.0 + 3 fields + TRADE_TYPE_NAME 14-entry)
- `src/data_downloader/storage/parquet_writer.py` (AC2 — `_check_no_field_drop`, SchemaIntegrityError)
- `src/data_downloader/orchestrator/orchestrator.py` (AC1 — `_to_schema_trade` preenche 3 campos)
- `src/data_downloader/dll/wrapper.py` (AC6 — comentário Q-DRIFT-35 NL_NOT_FOUND)
- `src/data_downloader/dll/callbacks.py` (AC7 — `make_history_trade_callback_v2(stats=...)`)
- `src/data_downloader/orchestrator/download_primitive.py` (AC7 — TRADE_QUEUE_MAXSIZE 2M, ChunkResult.queue_dropped, callback_stats wiring)
- `docs/storage/SCHEMA.md` (AC5 — tabela 14 valores TTradeType + cross-ref `schema.py::TRADE_TYPE_NAME`)
- `tests/unit/test_storage_schema_v110.py` (AC1+AC2 — 9 testes)
- `tests/unit/test_translate_failures_split.py` (telemetria — 5 testes)
- `tests/unit/test_dll_callbacks.py` (AC7 — 2 testes novos: counter + backward-compat)
- `tests/unit/test_ingestor_thread_sentinel.py` (Q-DRIFT-34 guard — 3 testes)

## QA Results

(a ser preenchido por Quinn pós-implementação)
