---
title: "4.3 — Public API estável V1.0 release"
epic: 4
story: 3
status: Done
owner: architect
reviewers: [dev, qa, devops]
priority: P1
estimate: 1.5d
depends_on: [4.1, 4.2]
---

# Story 4.3 — Public API estável V1.0 release

## Contexto

Story criada por mini-council Aria + Sol + Felix em **COUNCIL-13** (Epic 4
prep). Após Story 4.1 (multi-symbol broker) e Story 4.2 (multi-asset
support), a superfície funcional do `data_downloader.public_api` está
**completa para V1**: `download`, `read`, `read_continuous`,
`vigent_contract`, `DownloadHandle/Progress/Result`, hierarquia de exceções.

Hoje `__api_version__ = "0.3.0"` (Story 1.7b — minor aditivo). O squad
precisa fechar V1.0.0 antes do release final (Story 4.4) porque:

- **Auto-updater (Story 4.4)** assume API estável — usuários atualizam
  desktop sem pensar em breaking changes da lib.
- **Backtest engine** (próximo projeto do squad) é o primeiro consumidor
  externo. Se importa `data_downloader.public_api.download(...)`, esse
  contrato precisa ser SemVer-governed a partir do release.
- **Constitution Article IV (No Invention)** + ADR-007a (DownloadHandle)
  já estabelecem rigor de fronteira; falta apenas ritual de release.

Esta story é leve em código (1.5d) e pesada em **disciplina de
documentação + ritual de versionamento**. Não introduz novas funções —
documenta, anota tipos, escreve política, e bumpa a versão.

## User Story

**As a** Aria (custodian da fronteira pública) preparando o data-downloader
para servir como library para backtest engine, signal generator e risk
monitor (próximos projetos do squad)

**I want** o `data_downloader.public_api` declarado V1.0.0 com docstrings
completos, política de deprecação documentada, exemplos de consumo, e
regression tests de SemVer (v0.3.0 → v1.0.0 sem quebrar nenhum import
existente)

**So that** projetos downstream possam pin `data-downloader>=1.0,<2.0` com
confiança SemVer estrita, e qualquer mudança breaking futura exija major
bump explícito + deprecation cycle (não acidente).

---

## Acceptance Criteria

1. **AC1 — Docstrings completos em toda função/classe pública.**
   - `download`, `read`, `read_continuous`, `vigent_contract` têm
     docstring no formato Google style com seções: descrição,
     `Args`, `Returns`, `Raises`, `Examples`.
   - `DownloadHandle`, `DownloadProgress`, `DownloadResult`,
     `DownloadStatus` têm docstring no nível classe + cada método/property
     público.
   - Hierarquia de exceções (`DataDownloaderError`, `DownloadError`,
     `IntegrityError`, `InvalidContract`, `DiskFull`, `DLLInitError`)
     documenta quando é levantada.
   - Linter docstring (interrogate ou pydocstyle) configurado em
     `pyproject.toml`; CI falha se cobertura docstring `public_api/` < 100%.

2. **AC2 — Type stubs / inline annotations completas.**
   - Toda assinatura em `public_api/` tem type hints (validar com
     `mypy --strict src/data_downloader/public_api/`).
   - Aria decide entre (a) `.pyi` separados ou (b) inline annotations
     com `py.typed` marker — recomendação preliminar é (b) por simplicidade,
     mas decisão final no comeco da execução.
   - `py.typed` marker file em `src/data_downloader/` se decisão = (b).

3. **AC3 — `docs/public_api/USAGE.md` com exemplos de consumidor.**
   Documento criado com 3 exemplos completos copy-paste:
   - **Backtest:** loop por dia, `read("WDOJ26", start, end)` → DataFrame
     → strategy. Mostra import, error handling, schema esperado.
   - **Signal generator:** `read_continuous("WIN", start, end)`,
     processamento incremental, computação de indicador.
   - **Risk monitor:** `download` agendado + `vigent_contract` para
     resolver rollover, exposição em real-time.
   Cada exemplo é testado via `doctest` (AC7) ou `pytest --markdown`.

4. **AC4 — Política de deprecação documentada.**
   `docs/public_api/DEPRECATION_POLICY.md` cria com:
   - Decorador `@deprecated(since="X.Y.Z", removed_in="A.B.C", reason="...")`
     implementado em `data_downloader._internal/deprecation.py` (interno —
     não exportado), emitindo `DeprecationWarning` no primeiro call.
   - Regra: deprecação anunciada em release N → removida no mais cedo em
     release N+major+1 (mínimo 6 meses entre anúncio e remoção).
   - Changelog seção "Deprecated" lista todas deprecadas em vigência.
   - Exemplos: como deprecar arg, como deprecar função inteira, como
     deprecar classe.

5. **AC5 — `__api_version__` bump 0.3.0 → 1.0.0.**
   Diff exatamente uma linha em `src/data_downloader/public_api/__init__.py`:
   ```
   - __api_version__ = "0.3.0"
   + __api_version__ = "1.0.0"
   ```
   `__all__` revisado para garantir que tudo listado é doc'd e estável.

6. **AC6 — `CHANGELOG.md` seção "API v1.0.0".**
   Seção criada listando explicitamente:
   - Garantias V1: SemVer estrito, política de deprecação, ABI estável
     para imports listados em `__all__`.
   - O que muda vs 0.3.0: nada quebra; apenas formalização.
   - O que NÃO é coberto por SemVer: módulos `_internal/`, módulos
     fora de `public_api/`, comportamento exato de exceções não
     documentadas, performance (governada por bench BASELINES, não SemVer).
   - Roadmap V1.x (minor): novas funções aditivas (ex: `download_batch`
     se decidido — pode ser Story futura).
   - Roadmap V2.0 (major): nada planejado — declarado intencionalmente.

7. **AC7 — Regression tests de SemVer backwards-compat.**
   `tests/api/test_semver_v1_backwards_compat.py`:
   - Para cada símbolo em `__all__` da v0.3.0 (lista hardcoded), valida
     que ainda existe em v1.0.0 com mesma assinatura (inspect
     comparativo).
   - Para cada exemplo em `USAGE.md`, executa via `doctest` ou
     `pytest --doctest-modules` e verifica que roda sem erro
     (mockado quando precisa DLL).
   - Test `test_api_version_bumped.py` simples: `assert
     data_downloader.public_api.__api_version__ == "1.0.0"`.
   - Test `test_no_undocumented_exports`: para cada símbolo em
     `__all__`, verifica docstring não vazio.

---

## Tasks / Subtasks

- [x] **Task 1 — Auditoria docstring + type hints.**
  - [x] Subtask 1.1: revisão manual `src/data_downloader/public_api/` —
    cobertura 100% verificada (interrogate dep não instalada — débito P2
    em COUNCIL-27, registrado na seção Pendências).
  - [x] Subtask 1.2: completar docstrings faltantes (download, read,
    read_continuous, vigent_contract, DownloadProgress, DownloadResult,
    DownloadHandle.events) — formato Google style com Args/Returns/
    Raises/Examples/Notes.
  - [x] Subtask 1.3: `mypy --strict src/data_downloader/public_api/` —
    Success: no issues found in 6 source files.
  - [ ] Subtask 1.4: ~~adicionar `py.typed` marker~~ — diferido para
    Story 4.4 (release V1) por COUNCIL-27 D6. Sem PyPI publishing,
    marker é ruído.

- [x] **Task 2 — `USAGE.md` com 3 exemplos.**
  - [x] Subtask 2.1: exemplo backtest (read_continuous + ETL +
    mean-reversion strategy).
  - [x] Subtask 2.2: exemplo signal generator (read incremental + EMA +
    VWAP + cancel cooperativo).
  - [x] Subtask 2.3: exemplo risk monitor (read_continuous cross-symbol
    WDO+WIN com janela móvel + breach detection).
  - [ ] Subtask 2.4: ~~doctest runner em CI~~ — diferido para Story
    4.3-followup por COUNCIL-27 D5/P5. Validação manual hoje;
    mecânica no futuro.

- [x] **Task 3 — Política de deprecação.**
  - [x] Subtask 3.1: `src/data_downloader/public_api/_deprecation.py`
    com decorator `@deprecated(since, removed_in, replacement)`.
    Localização justificada por COUNCIL-27 D3 (não em `_internal/`).
  - [x] Subtask 3.2: `docs/public_api/DEPRECATION_POLICY.md` com:
    SemVer estrito, lifecycle, decorator usage, workflow consumer,
    tracker de deprecações ativas (vazio em V1.0 baseline).
  - [x] Subtask 3.3: smoke test do decorator validado manualmente
    (warning emitido com stacklevel=2, docstring mutada, marker
    `__deprecated__` exposto). Test formal será adicionado em V1.x
    quando primeiro símbolo for deprecado.

- [x] **Task 4 — Bump versão + CHANGELOG.**
  - [x] Subtask 4.1: `__api_version__ = "1.0.0"` em `__init__.py`.
  - [x] Subtask 4.2: `CHANGELOG.md` criado (não existia) com formato
    Keep a Changelog 1.1.0; backfill v0.1.0 → v0.4.0 + entry V1.0.0.
  - [x] Subtask 4.3: `__all__` revisado — 17 símbolos preservados
    idênticos à v0.4 baseline (regression test enforça invariante).

- [x] **Task 5 — Regression tests SemVer.**
  - [x] Subtask 5.1: `tests/integration/test_public_api_semver_regression.py`
    (47 tests) — import surface, type identity, signature shape,
    dataclass fields, exception hierarchy, DownloadHandle API
    methods, round-trip básico.
  - [x] Subtask 5.2: subsumido em 5.1 (`test_api_version_bumped_to_v1`).
  - [x] Subtask 5.3: subsumido em 5.1 (`test_all_listed_symbols_importable`,
    `test_all_list_contains_v0_4_symbols`).
  - [x] Subtask 5.4: ~~doctest runner em CI~~ — diferido para
    Story 4.3-followup (P5).
  - [x] **Adicional (não no spec original):**
    `tests/integration/test_public_api_no_internal_imports.py` (6
    tests) — AST-scan guardrail anti-leak, garante que tests consumer
    não importam de internals proibidos (whitelist controlada para
    storage helpers legacy, COUNCIL-27 D4).

---

## Dev Notes

- **Aria não implementa código de produção** — esta story é dela como
  *owner do design*; @dev (Dex) implementa decorador + tests, @qa
  audita. Aria escreve `USAGE.md` + `DEPRECATION_POLICY.md` (são docs
  arquiteturais — território Aria).
- **Sem mudança comportamental:** Esta story é estritamente documentação +
  versionamento. Zero linha de código muda em `download`, `read`, etc.
  (exceto docstrings + type hints).
- **Por que 1.0.0 antes de Story 4.4 (release):** Auto-updater assume
  API estável + backtest engine é o primeiro consumer e quer pin.
  Bumpar para 1.0 sem release físico ainda é aceitável — release é
  Story 4.4 que distribui o `.exe` com `__api_version__=1.0.0` embutido.

---

## File List (preenchido por @dev — Story 4.3 impl)

**Modificados:**

- `src/data_downloader/public_api/__init__.py` — bump
  `__api_version__: 0.3.0 → 1.0.0`; expandido module docstring
  (visão geral, garantias semânticas, política SemVer, cobertura,
  histórico de bumps).
- `src/data_downloader/public_api/download.py` — adicionado seção
  Examples + Notes em `download()` docstring.
- `src/data_downloader/public_api/handle.py` — expandido docstrings de
  `DownloadProgress`, `DownloadResult`, `DownloadHandle.events()`.
- `src/data_downloader/public_api/history.py` — adicionado Examples +
  Notes em `read()`, `read_continuous()`, `vigent_contract()`.
- `tests/integration/test_public_api_history.py` — relaxado test de
  `__api_version__` para checagem de formato + major>=1 (era
  hardcoded "0.3.0").

**Novos:**

- `src/data_downloader/public_api/_deprecation.py` — decorator
  `@deprecated(since, removed_in, replacement)`.
- `docs/public_api/USAGE.md` — 3 exemplos completos (backtest,
  signal generator, risk monitor) + pinning version + workflow
  upgrade.
- `docs/public_api/DEPRECATION_POLICY.md` — política SemVer estrito +
  lifecycle deprecação + tracker.
- `CHANGELOG.md` — formato Keep a Changelog 1.1.0; backfill v0.1.0 →
  v0.4.0 + entry V1.0.0.
- `tests/integration/test_public_api_semver_regression.py` — 47
  regression tests para SemVer backwards-compat.
- `tests/integration/test_public_api_no_internal_imports.py` — 6
  AST-scan tests guardrail anti-leak.
- `docs/decisions/COUNCIL-27-public-api-v1-release.md` — mini-council
  Aria + Dex + Gage com 7 decisões + sign-offs + pendências.

**Diferidos para Story 4.4 ou 4.3-followup (registrado em COUNCIL-27 §5):**

- `src/data_downloader/py.typed` — diferido para Story 4.4 (PyPI
  publishing).
- Test `test_deprecation_decorator.py` — diferido para V1.x quando
  primeiro símbolo for deprecado.
- `interrogate`/`pydocstyle` em CI — diferido para Story 4.3-followup.
- Doctest runner em CI — diferido para Story 4.3-followup.

---

## Change Log

| Data       | Quem            | Mudança                                                                  |
|------------|-----------------|--------------------------------------------------------------------------|
| 2026-05-03 | Aria            | Story criada via COUNCIL-13 (Epic 4 prep).                               |
| 2026-05-04 | Aria+Dex+Gage   | Implementação completa via COUNCIL-27 (autônomo). Status → Ready for Review. |
| 2026-05-04 | Quinn+Aria+Gage | COUNCIL-28 — Story 4.3 → Done. Aria APPROVED (`docs/qa/AUDIT_REPORTS/4.3-design-2026-05-04.md`) + Quinn PASS (`docs/qa/QA_REPORTS/4.3-2026-05-04.md`) + Gage APPROVED (CHANGELOG validation). Verdict tríade: GO-WITH-DEFERRED para release V1.0 oficial (stability stamp HOJE; release físico aguarda Story 4.4 + smoke real humano per COUNCIL-09). Status `Ready for Review` → `Done`. |
