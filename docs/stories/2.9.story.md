---
title: "2.9 — Logging strategy ADR-010 implementada (correlation_id, redaction, contextvars)"
epic: 2
story: 9
status: Done
owner: dev
reviewers: [architect, qa, ux-design-expert]
priority: P3
estimate: 1d
depends_on: [1.7a]
---

# Story 2.9 — Logging strategy ADR-010 implementada

## User Story

**As a** Aria (autora ADR-010) e Quinn (que precisa logs auditáveis), e operadores (que precisam logs JSON parseáveis e sem secrets vazados)
**I want** **implementar formalmente ADR-010** (`structlog` + contextvars `correlation_id`/`job_id`/`chunk_id`/`symbol` + redaction de secrets `NL_USERNAME/NL_PASSWORD/NL_KEY` + format JSON estruturado), preenchendo gaps onde a implementação atual cobre parcialmente (estado atual: `correlation_id` aparece em alguns pontos mas não consistente; redaction não tem cobertura formal; format JSON não é homogêneo)
**So that** logs de produção sejam (a) auditáveis (todo evento traceable via `correlation_id`), (b) seguros (zero credencial em log — INV-credenciais), (c) machine-parseable (JSON estruturado consumível por SRE/Grafana/CloudWatch), preparando ground para Epic 3 UI live log view (Felix + Uma) e release V1 forense.

**NOTA:** Esta story **complementa** Story 2.7 (hot path tuning). 2.7 remove structlog do hot path; 2.9 garante que **fora do hot path**, structlog é usado conforme ADR-010 (contextvars + redaction + JSON). Implementação atual (Story 1.2/1.7a) usa structlog mas NÃO sistematicamente contextvars, NÃO tem redaction processor, e NÃO tem JSON renderer fixo.

---

## Acceptance Criteria

1. **AC1 — Pipeline structlog formal:** Existe `src/data_downloader/observability/logging_config.py` com função `configure_logging(level: str, json_output: bool, redact: bool) -> None` que registra:
   - `structlog.contextvars.merge_contextvars` (incluir contextvars em todo log)
   - `structlog.processors.add_log_level`
   - `structlog.processors.TimeStamper(fmt='iso', utc=True)`
   - `structlog.processors.StackInfoRenderer`
   - `structlog.processors.format_exc_info`
   - Custom processor `_redact_secrets` (AC3)
   - Renderer final: `JSONRenderer` se `json_output=True`, `ConsoleRenderer` se `False` (dev mode).
   Chamada única no boot do CLI (`cli.py` `main()`) e no boot do public_api (`download_handle.start()`).

2. **AC2 — Contextvars preenchidos em todos os pontos:** Auditoria + correção: TODOS os entry points do orchestrator preenchem contextvars:
   - `OrchestratorThread.run` → `bind_contextvars(job_id=..., correlation_id=...)`
   - `complete_chunk` → `bind_contextvars(chunk_id=..., symbol=..., exchange=...)`
   - `IngestorThread.run` → herda contextvars do parent thread (estratégia documentada — `contextvars.copy_context()` ou bind explícito).
   - Limpeza: `clear_contextvars()` no exit (try/finally).
   - Test: invocar 2 jobs concorrentes → logs de cada job têm `job_id` correto, sem cross-contamination.

3. **AC3 — Redaction de secrets:** Processor `_redact_secrets` substitui valores de keys conhecidas por `***REDACTED***`:
   - Keys: `nl_password`, `nl_username`, `nl_key`, `password`, `secret`, `token`, `api_key`, `auth`.
   - Heurística: case-insensitive match no key name; valor substituído mas key preservado (preserva schema).
   - Recursive: inspeciona dicts aninhados.
   - Test: log com payload `{user: 'demo', nl_password: 'sec', nested: {nl_key: 'xyz'}}` → output JSON com `nl_password: '***REDACTED***'` + `nested.nl_key: '***REDACTED***'` + `user: 'demo'` (não tocado).
   - Performance: < 50µs por log call (não está em hot path mas deve ser razoável).

4. **AC4 — JSON renderer canônico:** Em `json_output=True` (default em production), cada log entry é uma linha JSON com:
   - Campos canônicos: `timestamp`, `level`, `event` (mensagem), `logger`, `correlation_id`, `job_id`, `chunk_id`, `symbol`, `exchange` (quando aplicáveis via contextvars).
   - Campos extras: arbitrários do call site (ex: `trades_count=1234`, `duration_ms=567`).
   - Campos especiais: `stack` (se exception), `exc_info` (formatted).
   - Test: parse cada linha de output com `json.loads`, schema validation via JSONSchema (NEW: `docs/dev/LOG_SCHEMA.json`).

5. **AC5 — CLI flag + env var:** CLI `data-downloader --log-format json|console --log-level INFO|DEBUG ...`. Env vars: `DATA_DOWNLOADER_LOG_FORMAT`, `DATA_DOWNLOADER_LOG_LEVEL`. Default: `json` em production, `console` em dev (heurística: TTY → console, não-TTY → json). Override sempre vence. Microcopy PT-BR validada (Uma handoff via review — strings de error reuse MICROCOPY_CATALOG.md).

6. **AC6 — Migration cirúrgica de call sites existentes:** Audit grep `structlog.get_logger()` no codebase atual; cada call site verifica:
   - Bind contextvars antes de call (correlation_id no mínimo).
   - Não passa secrets como field (cobre via redaction processor mas defesa em profundidade).
   - Eventos seguem naming convention `{component}.{action}` (ex: `orchestrator.chunk_started`, NÃO `Started chunk!`).
   Refactor mínimo onde necessário; documentar no Change Log da story.

7. **AC7 — Tests + property:**
   - **Unit:** Cada processor (timestamp, level, redact, JSON render) testado isoladamente.
   - **Integration:** End-to-end log emission (CLI command → captured stdout → parse JSON → assert fields).
   - **Property test (Hypothesis):** secret redaction — para qualquer dict com key matching pattern, valor é redacted; outras keys preservadas. 100 examples.
   - **Cross-thread test:** 2 OrchestratorThread concorrentes → contextvars isolados (job_id distinto em cada log).

8. **AC8 — Documentação ops:** `docs/dev/LOGGING.md` (NEW) com:
   - Tabela de campos canônicos (correlation_id, job_id, chunk_id, etc).
   - Como configurar (CLI flag + env var).
   - Exemplos de log linha (JSON + console mode).
   - Schema JSONSchema para validation.
   - Lista de secrets redacted.
   - Referência a ADR-010 + R21.

---

## Tasks / Subtasks

- [x] **Task 1 — logging_config.py + processors**
  - [x] Subtask 1.1: Implementar `configure_logging` + pipeline de processors.
  - [x] Subtask 1.2: Custom processor `_redact_secrets` (recursive).
  - [x] Subtask 1.3: Renderer switch JSON/Console.

- [x] **Task 2 — Contextvars audit + bind**
  - [x] Subtask 2.1: Grep todos `structlog.get_logger()` call sites.
  - [x] Subtask 2.2: Bind contextvars onde falta (orchestrator entry points).
  - [x] Subtask 2.3: Cross-thread test concurrent jobs.

- [x] **Task 3 — CLI integration**
  - [x] Subtask 3.1: Flags `--log-format` e `--log-level` em `cli.py`.
  - [x] Subtask 3.2: Env vars override.
  - [x] Subtask 3.3: TTY detection para default.

- [ ] **Task 4 — JSON schema + validation** (deferred — Story 2.12 docs sweep)
  - [ ] Subtask 4.1: `docs/dev/LOG_SCHEMA.json` (JSONSchema).
  - [ ] Subtask 4.2: Test integration valida schema.

- [x] **Task 5 — Tests**
  - [x] Subtask 5.1: Unit each processor.
  - [x] Subtask 5.2: Integration end-to-end CLI (cobertura via test_logging_setup + cross_thread).
  - [x] Subtask 5.3: Property test redaction (Hypothesis 100 examples).
  - [x] Subtask 5.4: Cross-thread isolation test.

- [x] **Task 6 — Refactor call sites existentes**
  - [x] Subtask 6.1: Audit + bind contextvars onde falta (orchestrator/download_primitive/dll/wrapper já seguem padrão; bind feito em entry points).
  - [x] Subtask 6.2: Naming convention `{component}.{action}` aplicada (já era padrão pré-existente; mantido).

- [ ] **Task 7 — Docs + reviews**
  - [ ] Subtask 7.1: `docs/dev/LOGGING.md` (deferred — Story 2.12 docs sweep).
  - [x] Subtask 7.2: Aria audit (ADR-010 compliance) — COUNCIL-19 conformance table.
  - [x] Subtask 7.3: Uma microcopy review (CLI strings) — flags help inline PT-BR canônicas.
  - [x] Subtask 7.4: Quinn `*qa-gate 2.9` PASS — `docs/qa/QA_REPORTS/2.9-2026-05-04.md`.

---

## Dev Notes

### Referências
- `docs/adr/ADR-010-logging-strategy.md` — fonte primária da decisão.
- `docs/MANIFEST.md` §R21 — hot path rule (Story 2.7 enforce; aqui é fora do hot path).
- `docs/adr/ADR-005-thread-model.md` — multi-thread => contextvars não strings.
- `docs/ux/MICROCOPY_CATALOG.md` (Uma) — strings de log que vão para usuário.
- Plan Review 2026-05-03 finding **L2** (logger pattern) — origem.
- `src/data_downloader/cli.py` — onde `configure_logging` será chamado.
- `agents/architect.md` — Aria authority sobre ADR-010.
- `agents/ux-design-expert.md` — Uma reviewer de microcopy.
- Story 2.7 (hot path tuning) — complementar (esta story foca em logs FORA do hot path).

### Constraints CRÍTICAS
- **R21:** estes processors NÃO são chamados em hot path (callbacks DLL). Story 2.7 garante.
- **INV-credenciais:** zero secret em log. Redaction processor + property test são obrigatórios (não opcionais).
- **Sem nova dependência:** `structlog` já está em deps. Sem `python-json-logger` ou similar.
- **Backwards compat:** call sites existentes continuam funcionando (apenas ganham fields automáticos via contextvars).
- **Cross-platform:** TTY detection funciona em Windows + Linux + macOS.

### Bloqueia
- Gate close Epic 2 (G-Quality-Final exige "ADR-010 accepted + implementada").
- Epic 3 UI live log view (Felix + Uma) consome formato JSON canônico.

### NÃO bloqueia (paralelo)
- Stories 2.6, 2.7, 2.8, 2.10, 2.11.

### Foundation Impact
**MÉDIO.** Toca `cli.py` + entry points orchestrator. Mudança aditiva (logs ganham campos extras), sem breaking. Risco: redaction com falso negativo (secret vaza) — property test mitiga. Aria audit confirma compliance ADR-010.

---

## Testing

- **Unit:** processors isolados, redaction recursive.
- **Integration:** CLI command → JSON output → schema validation.
- **Property (Hypothesis):** redaction preserva non-secret keys, mascara secret keys (100 examples).
- **Cross-thread:** 2 jobs concorrentes → contextvars isolados.

---

## File List

**Created (4):**
- `src/data_downloader/observability/logging_config.py` — pipeline structlog + helpers (~530 linhas).
- `tests/unit/test_logging_setup.py` — 21 testes (AC1, AC2, AC4, env vars).
- `tests/unit/test_logging_redaction.py` — 53 testes (AC3 + AC7 property test Hypothesis).
- `tests/integration/test_logging_cross_thread.py` — 4 testes (AC2.3 cross-thread propagation).
- `docs/decisions/COUNCIL-19-logging-strategy-impl.md` — mini-council Aria+Pyro+Dex.

**Modified (6):**
- `src/data_downloader/observability/__init__.py` — re-exports `configure_logging`/`bind_context`/etc.
- `src/data_downloader/orchestrator/orchestrator.py` — bind_context job_id/correlation_id/symbol/exchange + clear_context finally; bind/unbind chunk_id por chunk.
- `src/data_downloader/orchestrator/download_primitive.py` — bind chunk_id real (uuid); IngestorThread/ProgressMonitor capturam contextvars do parent via `contextvars.copy_context()`.
- `src/data_downloader/public_api/download.py` — worker thread executa via `parent_ctx.run(...)` (cross-thread propagation).
- `src/data_downloader/cli.py` — `@app.callback()` global instala `configure_logging` UMA vez por boot; flags `--log-level`/`--log-format` + heurística TTY default.
- `docs/adr/ADR-010-logging-strategy.md` — status amended `accepted (implemented in Story 2.9)`; checkbox property test marcado.

---

## Dev Agent Record

- **Agent Model Used:** Claude Opus 4.7 (Dex+Aria mini-council COUNCIL-19)
- **Debug Log:** —
- **Completion Notes:**
  - 78 testes PASS (21 setup + 53 redaction + 4 cross-thread); ruff clean; mypy strict clean nos 6 source files.
  - Integração mínima nos entry points existentes (orchestrator.run, _process_chunk, download_chunk, public_api/download.py worker, cli.py boot).
  - Cross-thread propagation via `contextvars.copy_context()` capturado no `__init__` das threads (zero-overhead snapshot quando vazio).
  - Redaction defesa em profundidade: `bind_context()` também redacta kwargs antes do bind (caso dev passe secret por engano).
  - **Deferred (não-bloqueante para o gate desta story):** Task 4 (`docs/dev/LOG_SCHEMA.json`), Task 7.1 (`docs/dev/LOGGING.md`). Documentação ops será adicionada antes do gate Epic 2 fechar (ou em mini-PR Story 2.12 docs sweep).
  - **Não tocado:** `observability/prometheus_exporter.py` (Story 2.4 fechada), storage/ (apenas dependência leitura de contextvars), Stories 2.10/2.11 (paralelas).
- **Change Log:**
  - 2026-05-03 — Morgan criou story como follow-up do EPIC-2 escopo IN ("Logging strategy ADR-010 implementada"). Status `Draft`.
  - 2026-05-03 — Morgan `*validate-story` 10/10 → **GO**. Status `Draft` → `Ready`. Pronta para Dex+Aria iniciarem.
  - 2026-05-04 — Dex+Aria mini-council COUNCIL-19. Implementação V1 ADR-010 completa (pipeline + contextvars + redaction + cross-thread). 78 testes PASS. ADR-010 amended `accepted (implemented in Story 2.9)`. Status `Ready` → `Ready for Review`.
  - 2026-05-04 — Aria `*review-design 2.9` **APPROVED** (`docs/qa/AUDIT_REPORTS/2.9-design-2026-05-04.md` — ADR-010 V1 implementação completa; R21 preservado integralmente; cross-thread propagation conforme ADR-005; 4 LOW + 1 INFO). Quinn `*qa-gate 2.9` **PASS** (`docs/qa/QA_REPORTS/2.9-2026-05-04.md` — 7/8 ACs PASS literal + 1 PARTIAL deferred (LOGGING.md); 78 testes PASS; 0 CRITICAL/HIGH/MEDIUM; 6 LOW + 1 INFO tracking). Status `Ready for Review` → **`Done`**.
