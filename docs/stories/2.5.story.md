---
title: "2.5 — Calendar B3 holidays.dat Integration"
epic: 2
story: 5
status: Ready for Review
owner: storage-engineer
reviewers: [profitdll-specialist, qa, dev]
priority: P2
estimate: 2d
depends_on: [2.1]
---

# Story 2.5 — Calendar B3 holidays.dat Integration

## User Story

**As a** sistema (DataValidator gap detection + IntegrityChecker `check_no_unexpected_gaps` + futuros validators de calendário)
**I want** ler o arquivo oficial `profitdll/DLLs/Win64/holidays.dat` distribuído pela Nelogica e usá-lo como fonte de calendário B3 em runtime
**So that** a tabela hardcoded de feriados 2025-2026 (`src/data_downloader/validation/calendar_b3.py`) seja substituída por fonte autoritativa e atualizável (cobre histórico ≥ 2020 + futuro até onde Nelogica documenta), eliminando finding **F-S-1** do Sol audit Story 2.1 e **COUNCIL-04 caveat**.

**NOTA:** Esta story executa o TODO documentado no docstring de `calendar_b3.py`: "integrar com `holidays.dat` distribuído pela Nelogica (PROFITDLL_KNOWLEDGE.md §X) — fonte autoritativa". Mantém fallback hardcoded para ambientes dev sem ProfitDLL instalado (CI, contributors externos).

---

## Acceptance Criteria

1. **AC1 — Investigação de formato:** Antes de implementar parser, Nelo (profitdll-specialist) confirma formato do `holidays.dat` consultando manual oficial (`Nelo *manual --section ?` se existir seção) ou via reverse engineering best-effort com hexdump + análise byte-a-byte. Resultado documentado em `docs/dll/HOLIDAYS_DAT_FORMAT.md` (ver AC7). Se formato é proprietário binário sem doc, registrar `validation_source: reverse_engineered` + lista de hipóteses testadas.

2. **AC2 — Parser dedicado:** Existe `src/data_downloader/validation/holidays_dat_parser.py` com:
   - Função `parse_holidays_dat(path: Path) -> frozenset[date]` — lê arquivo, retorna conjunto de feriados.
   - Tratamento de erros: arquivo ausente raise `HolidaysDatNotFoundError`; arquivo malformado raise `HolidaysDatParseError` com offset + bytes problemáticos.
   - Sem dependência nova (use só `struct` / `pathlib` / stdlib).

3. **AC3 — Integração transparente em `is_b3_business_day`:** `calendar_b3.py` agora consulta `holidays.dat` se disponível; fallback para tabela hardcoded se não. Lógica:
   ```
   if HOLIDAYS_DAT_PATH.exists():
       holidays = parse_holidays_dat(HOLIDAYS_DAT_PATH)  # cache módulo
   else:
       holidays = _B3_HOLIDAYS_2025_2026  # fallback
   ```
   API pública (`is_holiday`, `is_b3_business_day`, `b3_business_days_range`) **NÃO muda** — apenas fonte interna.

4. **AC4 — Cobertura mínima de feriados 2020-2030:** Após parsing de `holidays.dat` real (ou stub fixture com layout descoberto na AC1), conjunto retornado contém pelo menos os feriados nacionais brasileiros de 2020 até 2030 (≥ 13 feriados/ano × 11 anos = ≥ 143 entradas). Se cobertura efetiva é menor, log warning + complementar com hardcoded fallback dentro do range.

5. **AC5 — Refresh automático no boot + mtime check:** Ao primeiro uso, parser cacheia resultado + mtime do arquivo. Em invocações subsequentes, se mtime mudou, re-parse (Nelogica pode atualizar `holidays.dat` em update da DLL). Cache TTL adicional opcional (default 24h em background — Sol decide). Test verifica: alterar mtime artificialmente → próxima chamada re-parseia.

6. **AC6 — Test ground truth:** `tests/unit/test_holidays_dat_parser.py` valida contra dataset conhecido:
   - Feriados 2024 (passado, fonte oficial B3): 13 datas confirmadas.
   - Feriados 2025 (já hardcoded em `_B3_HOLIDAYS_2025_2026`): 13 datas confirmadas — devem bater 100%.
   - Feriados 2026 (já hardcoded): 13 datas confirmadas — devem bater 100%.
   - Caso edge: data não-feriado (ex: 2025-01-02) — `is_holiday(d)` retorna `False` em ambos os caminhos (parser + fallback).
   - Property test (Hypothesis): para qualquer date no range 2025-2026, `is_holiday(d)` via parser == `is_holiday(d)` via hardcoded.

7. **AC7 — Doc de formato:** `docs/dll/HOLIDAYS_DAT_FORMAT.md` documenta:
   - Localização do arquivo (`profitdll/DLLs/Win64/holidays.dat` ou outro path descoberto na AC1).
   - Formato byte-a-byte (header magic se existir, encoding de datas, alinhamento, endianness).
   - Validação contra fonte oficial Nelogica (`validation_source: nelogica_official` se manual confirmou; `reverse_engineered` se inferido).
   - Versão do `holidays.dat` analisada (sha256 + mtime).
   - Limitações conhecidas (range de anos coberto, ponto facultativo vs feriado oficial).
   - Nelo é reviewer obrigatório (DLL authority).

8. **AC8 — Graceful fallback:** Se `holidays.dat` não existe (ProfitDLL não instalado em CI ou contributor externo sem licença), API funciona normalmente com tabela hardcoded — log INFO `"holidays.dat não encontrado em {path}; usando tabela hardcoded 2025-2026"`. Test em CI confirma: sem `holidays.dat`, todos os tests existentes de `calendar_b3` continuam PASS sem mudança.

---

## Tasks / Subtasks

- [x] **Task 1 — Investigação de formato (Nelo handoff)**
  - [x] Subtask 1.1: Manual ProfitDLL não documenta `holidays.dat` (verificado em PROFITDLL_KNOWLEDGE.md §1-8).
  - [x] Subtask 1.2: Hexdump + análise byte-a-byte → texto plano UTF-8 BOM, formato `EE:DATE:OPEN:CLOSE:DESC` decifrado.
  - [x] Subtask 1.3: Layout documentado em `docs/dll/HOLIDAYS_DAT_FORMAT.md`.
  - [x] Subtask 1.4: Reverse engineering CONCLUSIVO — não houve necessidade de escalada. Fallback hardcoded estendido 2020-2030 implementado para complementar (Nelogica omite feriados em FDS). `validation_source: reverse_engineered`.

- [x] **Task 2 — Parser**
  - [x] Subtask 2.1: `parse_holidays_dat(path)` implementado em `validation/holidays_dat_parser.py`.
  - [x] Subtask 2.2: Exceções `HolidaysDatNotFoundError`, `HolidaysDatParseError` com offset/line.
  - [x] Subtask 2.3: Cache módulo + mtime check + thread lock.
  - [x] Subtask 2.4: Type hints completos + docstring PT-BR.

- [x] **Task 3 — Integração em calendar_b3**
  - [x] Subtask 3.1: `calendar_b3.py` carrega lazy + cache de conjunto efetivo.
  - [x] Subtask 3.2: `HOLIDAYS_DAT_ENV_VAR = "DATA_DOWNLOADER_HOLIDAYS_DAT_PATH"` + default `profitdll/DLLs/Win64/holidays.dat`.
  - [x] Subtask 3.3: Log INFO `calendar_b3.source` indica fonte ativa (`dat+hardcoded` ou `hardcoded_only`).

- [x] **Task 4 — Tests**
  - [x] Subtask 4.1: `tests/unit/test_holidays_dat_parser.py` — 13 testes (fixtures sintéticas + real DAT condicional).
  - [x] Subtask 4.2: `tests/integration/test_calendar_b3_holidays_dat.py` — 8 testes de integração (parser + fallback).
  - [x] Subtask 4.3: `tests/property/test_calendar_b3_consistency.py` — 5 testes Hypothesis (invariants).
  - [x] Subtask 4.4: Test refresh automático mtime — incluído em ambos parser e integration.
  - [x] Subtask 4.5: Test fallback sem arquivo — `test_arquivo_inexistente_usa_hardcoded`.
  - [x] Subtask 4.6 (extra): `tests/unit/test_calendar_b3_extended.py` — 50+ testes parametrizados cobertura 2020-2030.

- [x] **Task 5 — Doc HOLIDAYS_DAT_FORMAT.md**
  - [x] Subtask 5.1: Doc completo com formato, exemplos hex, ground truth comparison, validation_source.
  - [x] Subtask 5.2: Nelo audit aprovado em COUNCIL-16.

- [ ] **Task 6 — Reviews**
  - [x] Subtask 6.1: Nelo audit aprovado (COUNCIL-16, sign-off DLL authority).
  - [x] Subtask 6.2: Sol audit aprovado (COUNCIL-16, API preservada).
  - [ ] Subtask 6.3: Quinn `*qa-gate 2.5` — PENDING (story marcada Ready for Review).

---

## Dev Notes

### Referências
- `src/data_downloader/validation/calendar_b3.py` — TODO no docstring (linhas 8-11) — fonte do trabalho.
- `docs/qa/AUDIT_REPORTS/2.1-storage-2026-05-04.md` finding **F-S-1** (Sol) — origem.
- `docs/decisions/COUNCIL-04-*.md` — caveat sobre calendário hardcoded.
- `docs/storage/CONTRACTS.md` §0 — `validation_source` enum (reusado em HOLIDAYS_DAT_FORMAT.md).
- `docs/storage/INTEGRITY.md` §6 (M17) — DST B3 ambiguidade pré-2019; cobertura ≥ 2020 já é constraint.
- `docs/dll/PROFITDLL_KNOWLEDGE.md` (Nelo) — possível seção sobre arquivos auxiliares da DLL.
- `agents/profitdll-specialist.md` — Nelo authority sobre tudo que vem da Nelogica.
- `agents/storage-engineer.md` — Sol authority sobre validators / calendário.
- Story 2.1 — onde calendar_b3 nasceu hardcoded.
- Plan Review finding **M17** (DST B3) — boundary já estabelecida.

### Constraints CRÍTICAS

- **Zero alucinação (R23):** Se formato é incerto, Nelo NÃO chuta. Documenta como `reverse_engineered` + lista hipóteses testadas. Sem confirmação Nelogica oficial, parser é best-effort + fallback obrigatório.
- **Fallback obrigatório:** Sem `holidays.dat`, sistema continua funcional com tabela hardcoded. CI roda sem ProfitDLL — não pode quebrar.
- **API pública não muda:** `is_holiday`, `is_b3_business_day`, `b3_business_days_range` mantêm assinatura — apenas fonte interna troca.
- **DST boundary preservada:** Sol policy `>= 2020-01-01` para gap detection (M17) — calendar_b3 continua respeitando.
- **mtime check leve:** não re-parsear a cada chamada (perf). Cache + mtime check é o equilíbrio.
- **Nelo é reviewer obrigatório:** sem GO de Nelo, story NÃO vai a InReview (formato é DLL territory).

### Bloqueia

- Gate close Epic 2 (G-Quality-Final exige "Calendar B3 lê `holidays.dat` Nelogica em runtime").
- Future: extensão de DataValidator para validar gaps em datasets multi-anos (Epic 4 multi-asset).

### NÃO bloqueia (paralelo)

- Story 2.3 (migration framework), Story 2.4 (Prometheus exporter) — independentes.

### Foundation Impact

**MÉDIO.** Toca validation/ (Sol authority compartilhada com Quinn). Substituição de fonte é transparente para consumidores (API estável). Bug no parser = falsos positivos em gap detection (não-fatal mas confuso). Nelo audit + property test (parser vs hardcoded) são as principais defesas.

---

## Testing

- **Unit:**
  - `parse_holidays_dat(fixture_minimal.dat)` retorna conjunto esperado.
  - Arquivo malformado → `HolidaysDatParseError` com offset/bytes.
  - Arquivo ausente → `HolidaysDatNotFoundError`.
  - Cache + mtime: alterar mtime → re-parse.

- **Integration:**
  - `calendar_b3.is_holiday(d)` produz mesmo resultado com `holidays.dat` real (se disponível em dev box) e com fallback (CI).
  - `is_b3_business_day` continua respeitando weekend + holidays para datas 2024/2025/2026.

- **Property-based (Hypothesis):**
  - Para qualquer `d` no range `[2025-01-01, 2026-12-31]`: `is_holiday(d)` via parser == `is_holiday(d)` via hardcoded (100 examples).

- **Fallback test:**
  - Variável env `DATA_DOWNLOADER_HOLIDAYS_DAT_PATH=/nonexistent` → log INFO emitido + API funcional via hardcoded.

- **Smoke (manual, dev box com ProfitDLL):**
  - Sol roda `data-downloader integrity check --symbol WDOJ26` em dataset real; nenhum gap espúrio detectado em datas que parser identifica como feriado.

---

## File List

**Source:**
- `src/data_downloader/validation/holidays_dat_parser.py` (NEW — ~205 linhas)
- `src/data_downloader/validation/calendar_b3.py` (EDITED — estende hardcoded 2020-2030 + integração parser + fallback)
- `src/data_downloader/validation/__init__.py` (EDITED — export `b3_holidays`)

**Tests:**
- `tests/unit/test_holidays_dat_parser.py` (NEW — 13 testes)
- `tests/unit/test_calendar_b3_extended.py` (NEW — 50+ testes parametrizados 2020-2030)
- `tests/integration/test_calendar_b3_holidays_dat.py` (NEW — 8 testes)
- `tests/property/test_calendar_b3_consistency.py` (NEW — 5 testes Hypothesis)

**Docs:**
- `docs/dll/HOLIDAYS_DAT_FORMAT.md` (NEW — Nelo authority)
- `docs/decisions/COUNCIL-16-holidays-dat-integration.md` (NEW — mini-council Sol+Nelo+Dex)
- `docs/stories/2.5.story.md` (EDITED — status Ready for Review, tasks marcadas)

---

## Dev Agent Record

- **Agent Model Used:** Mini-council Sol (storage-engineer) + Nelo (profitdll-specialist) + Dex (dev) — modo autônomo (Opus 4.7).
- **Debug Log:**
  - Investigação formato: hexdump revelou texto plano UTF-8 BOM, primeira linha comentário com timestamp. Padrão `EE:YYYYMMDDHHMMSSF:OPEN:CLOSE:DESC` confirmado em 100% das 843 linhas.
  - Discovery: códigos `EE` correspondem a chars ASCII (66='B' Bovespa, 70='F' BMF, 35='#' B3 unified, 89='Y' NYSE, etc.). Apenas 35/66/70/88/99 são B3-related.
  - Quirk Nelogica: feriados em FDS NÃO são listados (provável: já não há pregão). Hardcoded estendido captura essas datas para completude.
  - Pontos facultativos (24/12, 31/12) são listados como full-day na DAT — incluídos na união.
  - Cinzas/vésperas têm `OPEN` preenchido (pregão parcial) → SKIPADAS em V1.
- **Completion Notes:**
  - Estratégia união (parser ∪ hardcoded) cobre superset semântico — gap detection conservadora.
  - Cache mtime-based em ambos parser e calendar — re-parse automático quando Nelogica atualizar DAT.
  - Fallback graceful: env var `DATA_DOWNLOADER_HOLIDAYS_DAT_PATH` + suporte a arquivo ausente/corrompido.
  - Mini-council COUNCIL-16 documentado (Sol+Nelo+Dex sign-offs).
  - Q16-OPEN: confirmação oficial Nelogica do formato fica como deferred (deve ser submetida via canal de suporte oficial).
- **Test Results:** 622 passed, 5 skipped na suite completa. Zero regressão Story 2.1.
- **Lint/Type:** ruff clean + mypy strict clean (5 source files no validation/).
- **Change Log:**
  - 2026-05-03 — Morgan criou story como follow-up de COUNCIL-10 + finding F-S-1 Sol audit 2.1. Status `Draft`.
  - 2026-05-03 — Morgan `*validate-story` 10/10 → **GO**. Status `Draft` → `Ready`. Pronta para Nelo (Task 1 investigação) + Sol/Dex (Tasks 2-6).
  - 2026-05-03 — Mini-council Sol+Nelo+Dex implementação autônoma. AC1-AC8 demonstradas. COUNCIL-16 documentado. Status `Ready` → `Ready for Review`.
