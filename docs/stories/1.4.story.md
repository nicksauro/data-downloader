---
title: "1.4 — Storage layer: writer Parquet + leitor DuckDB"
epic: 1
story: 4
status: Ready
owner: dev
reviewers: [storage-engineer, qa, architect]
priority: P0
estimate: 2d
depends_on: [0.0, 1.1]
---

# Story 1.4 — Storage layer: writer Parquet + leitor DuckDB

## User Story

**As a** orchestrator (Story 1.7)
**I want** uma camada de storage que recebe `list[TradeRecord]` (de Story 1.3), persiste em Parquet (Snappy, row_group=100k) com metadata `schema_version`, e expõe leitor via DuckDB
**So that** os trades baixados ficam queryáveis por projetos downstream sem perder schema-versioning ou integridade.

---

## Acceptance Criteria

1. **AC1:** Schema v1.0.0 implementado conforme `docs/storage/SCHEMA.md` (entregue por Story 0.0 — Sol). Inclui campos novos: `side` (uint8), `ingestion_ts_ns` (int64 NOT NULL), `chunk_id` (string NOT NULL), `dll_version` (string NOT NULL coletado via Story 1.1 wrapper).
2. **AC2:** `src/data_downloader/storage/parquet_writer.py` expõe classe `ParquetWriter` com método `write(trades: list[TradeRecord], partition: PartitionKey) -> WriteResult`.
3. **AC3:** `PartitionKey` dataclass: `exchange: str (1 char)`, `symbol: str`, `year: int`, `month: int`. `WriteResult`: `path: Path`, `row_count: int`, `first_ts_ns`, `last_ts_ns`, `checksum_sha256: str`.
4. **AC4:** Path resolvido conforme ADR-004: `data/history/{exchange}/{symbol}/{year}/{month:02d}.parquet`. Diretório criado se não existir.
5. **AC5:** Escrita atômica: escreve em `{path}.tmp.{uuid}` → fsync(file) → calcula SHA256 → `os.replace(tmp, path)` (atômico Windows + Linux) → **fsync(parent_dir)** (finding H7 — garante diretório persistido antes de catalog avançar).
6. **AC6:** Metadata Parquet inclui chaves: `schema_version` (`"1.0.0"`), `row_count`, `first_ts_ns`, `last_ts_ns`, `dll_version` (placeholder por enquanto), `created_at` (ISO).
7. **AC7:** Configuração Parquet conforme ADR-002: `compression='snappy'`, `row_group_size=100_000`, `use_dictionary=True`, `write_statistics=True`.
8. **AC8:** Se arquivo já existe na partição, `write` faz **append + dedup** com chave canônica `(symbol, exchange, timestamp_ns, sequence_within_ns)` (conforme SCHEMA.md Story 0.0 — finding H2 reformulado). Lei R5.

   **Threshold rewrite vs new file (finding H6 — evita O(n²) append):** se arquivo existente > N MB (default 256MB, configurável), em vez de reescrever, criar `MM.{N}.parquet` (sequência incrementada). DuckDB reader (AC9) usa glob `MM.*.parquet`. Detalhes em INTEGRITY.md (Story 0.0).
9. **AC9:** `src/data_downloader/storage/duckdb_reader.py` expõe `DuckDBReader` com método `read(symbol, start_ts_ns, end_ts_ns, exchange='F') -> pa.Table`. Usa `read_parquet('data/history/{exchange}/{symbol}/**/*.parquet')` com filtro `WHERE timestamp_ns BETWEEN start AND end ORDER BY timestamp_ns ASC`.
10. **AC10:** Hypothesis property test: `dedup(L ++ L) == dedup(L)` (INV-2).

   **REMOVIDO (movido para Story 2.1):** round-trip property test (`read(write(L)) == sorted_dedup(L)`) vai para Story 2.1 (validators como código). Aqui em 1.4 ficou só dedup idempotência.

## Tasks / Subtasks

- [ ] **Task 1 — Sol cria SCHEMA.md**
  - [ ] Subtask 1.1: Sol `*schema-doc` produz `docs/storage/SCHEMA.md` v1.0.0
  - [ ] Subtask 1.2: Dex consulta Sol `*schema --show` para confirmar campos antes de implementar
- [ ] **Task 2 — Implementar `storage/parquet_writer.py`**
  - [ ] Subtask 2.1: Dataclass `PartitionKey`
  - [ ] Subtask 2.2: Dataclass `WriteResult`
  - [ ] Subtask 2.3: Função privada `_resolve_partition_path(key, data_dir) -> Path`
  - [ ] Subtask 2.4: Função privada `_to_pyarrow_table(trades, schema_version) -> pa.Table`
  - [ ] Subtask 2.5: Função privada `_compute_sha256(path) -> str`
  - [ ] Subtask 2.6: Classe `ParquetWriter(data_dir: Path)` com `.write(trades, partition)`
  - [ ] Subtask 2.7: Lógica de append+dedup quando arquivo existe
- [ ] **Task 3 — Implementar `storage/dedup.py`**
  - [ ] Subtask 3.1: Função `dedup(trades: list[TradeRecord]) -> list[TradeRecord]` por `(symbol, timestamp_ns, trade_id)` — quando `trade_id` é None, fallback para hash canônico `(price, quantity, side?)`
  - [ ] Subtask 3.2: Property tests
- [ ] **Task 4 — Implementar `storage/duckdb_reader.py`**
  - [ ] Subtask 4.1: Classe `DuckDBReader(data_dir: Path)` com conexão DuckDB persistente
  - [ ] Subtask 4.2: Método `read(symbol, start_ts_ns, end_ts_ns, exchange) -> pa.Table`
  - [ ] Subtask 4.3: Método `count(symbol, exchange) -> int`
- [ ] **Task 5 — Schema Pyarrow**
  - [ ] Subtask 5.1: Definir schema pyarrow em `storage/schema.py` correspondente ao SCHEMA.md
  - [ ] Subtask 5.2: Constante `SCHEMA_VERSION = "1.0.0"`
  - [ ] Subtask 5.3: Função `validate_record(record: TradeRecord) -> None` (raises ValueError) — validações simples (price > 0, qty > 0, exchange ∈ {'F','B'})
- [ ] **Task 6 — Testes**
  - [ ] Subtask 6.1: `tests/unit/test_dedup.py` — Hypothesis: `dedup(L ++ L) == dedup(L)`, `dedup(dedup(L)) == dedup(L)`
  - [ ] Subtask 6.2: `tests/unit/test_partition_path.py` — resolve_partition_path para exemplos conhecidos
  - [ ] Subtask 6.3: `tests/unit/test_schema.py` — schema pyarrow corresponde ao SCHEMA.md
  - [ ] Subtask 6.4: `tests/integration/test_parquet_writer.py` — write + leitura via PyArrow direto + via DuckDB
  - [ ] Subtask 6.5: `tests/integration/test_atomicity.py` — simula crash entre write tmp e replace; verifica que cleanup funciona OU `.tmp` órfão é detectável (Story 1.5 implementa cleanup completo)
  - [ ] Subtask 6.6: `tests/property/test_roundtrip.py` — Hypothesis: `read(write(L)).to_pylist() == sorted_dedup(L)`
- [ ] **Task 7 — Auditoria**
  - [ ] Subtask 7.1: Sol `*audit-storage-pr src/data_downloader/storage/` — APPROVED obrigatório
  - [ ] Subtask 7.2: Quinn `*data-validate --symbol WDOJ26` (após smoke de Story 1.7 rodar — esta task fica registrada como dependência futura)

## Dev Notes

### Referências
- ADR-002 (storage stack) — config Parquet
- ADR-004 (partition layout)
- `agents/storage-engineer.md` — `expertise.schema_v1_proposal` é o schema base
- Lei R4 (schema versioning), R5 (idempotência), R6 (catálogo é fonte única — Story 1.5)

### Schema v1.0.0 (referência — Sol formaliza em SCHEMA.md)

| Campo | Tipo pyarrow | Nullable | Notas |
|-------|--------------|----------|-------|
| symbol | string | No | "WDOJ26" |
| exchange | string (size 1) | No | "F" ou "B" |
| timestamp_ns | int64 | No | nanos epoch BRT NAIVE (R7) |
| timestamp_str | string | No | "DD/MM/YYYY HH:mm:SS.ZZZ" original |
| price | float64 | No | > 0 |
| quantity | int64 | No | > 0 |
| trade_id | int64 | Yes | NULL se DLL não fornece |
| trade_type | uint8 | No | TConnectorTradeType |
| buy_agent_id | int32 | Yes | |
| sell_agent_id | int32 | Yes | |
| flags | uint32 | No | TC_IS_EDIT etc |
| source_callback | string | No | "history_v2" ou "history_v1" |

### Constraints CRÍTICAS
- **Atomicidade** — `os.replace` é atômico em ambos OS. Tmp file pattern essencial.
- **Dedup** chave é `(symbol, timestamp_ns, trade_id)` quando `trade_id` existe; senão hash canônico `(price, quantity, ?)`.
- **Append**: precisa ler existente, juntar, deduplicar, reescrever. Não tem append in-place no Parquet.
- **DuckDB conexão**: pode ser persistida (`DuckDBReader` mantém conn aberta).

### Decisão pendente (consultar Sol)
- Onde armazenar `dll_version`? Hard-coded em writer ou injetado via parâmetro? Sugestão Aria: parâmetro injetado pelo orchestrator.

## Testing

- **Unit:** dedup (property-based), partition path, schema, validate_record
- **Integration:** writer + reader round-trip
- **Property-based:** dedup idempotência (INV-2), round-trip ordenação (INV-7)
- **Smoke:** —

## File List

(preencher durante implementação)

## Dev Agent Record

- **Agent Model Used:**
- **Debug Log:**
- **Completion Notes:**
- **Change Log:**
