# Smoke Evidence — Attempt 9 (Story 1.7d, `minimal_handshake_strict`)

**Verdict:** **INCONCLUSIVE — smoke não executado**

**Data análise:** 2026-05-05 12:17 UTC
**Hash commit:** *N/A — implementação 1.7d ainda não commitada* (working tree dirty: `wrapper.py`, `download.py`, `test_download_primitive_real.py`, `test_dll_wrapper.py`)
**Executor:** Quinn (@qa) — análise pós-implementação Dex
**Hostname:** `<HOME>` (sanitizado)

## Achado primário

**Não existe log `smoke1-attempt9-*.log`.** A diretiva da Story 1.7d declarava que Dex rodaria o smoke após corrigir `minimal_handshake` para o espelho estrito do probe (slots 5/9/10 = REAL). Esta análise QA verificou:

| Verificação | Resultado |
|-------------|-----------|
| `Glob docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt9-*.log` | **0 arquivos** |
| `Glob **/smoke1-attempt9*` (todo repo) | **0 arquivos** |
| `Glob **/attempt9*` | **0 arquivos** |
| `Glob docs/qa/SMOKE_EVIDENCE/1.7d-*` (evidência prévia Dex) | **0 arquivos** (apenas este) |
| Log mais recente em `docs/qa/SMOKE_EVIDENCE/logs/` | `smoke1-attempt8-20260504T224457Z.log` (22:45 BRT, ontem) |
| Processo `python` rodando agora | **nenhum** (`Get-Process python` → vazio) |
| Working tree git | **dirty**, sem commit da implementação 1.7d |
| Story 1.7d tasks | Linhas 121-123 marcam `[x]` em "Rodar smoke attempt 9" e "Salvar evidência" |

**Conclusão:** Os checkboxes "Rodar smoke" e "Salvar evidência" estão marcados FALSAMENTE na Story 1.7d. Implementação foi feita (wrapper + testes), mas o smoke real **nunca rodou**. Não há evidência empírica para concluir nada sobre Q-DRIFT-12.

## Estado da implementação (verificado)

A correção do código está **correta** e atende AC1/AC2:

`src/data_downloader/dll/wrapper.py:738-786` (versão dirty no working tree):

```python
non_state_slots: list[Any] = [
    None,                                        # slot 4  newTradeCallback
    make_noop_callback(TDailyCallback),          # slot 5  newDailyCallback   REAL
    None,                                        # slot 6  newHistoryCallback
    None,                                        # slot 7  priceBookCallback
    None,                                        # slot 8  offerBookCallback
    make_noop_callback(TProgressCallback),       # slot 9  progressCallBack   REAL
    make_noop_callback(TTinyBookCallback),       # slot 10 tinyBookCallBack   REAL
]
```

Imports adicionados em wrapper.py: `TDailyCallback`, `TProgressCallback`, `TTinyBookCallback` (Q-DRIFT-05 alinhado).

Log evento esperado (quando rodar): `dll.initialize_call ... slots_active=['state','newDaily','progress','tinyBook'] slots_none=4 slots_real_noop=3 minimal_handshake=True story=1.7d quirk_under_test=Q-DRIFT-12`.

`tests/smoke/test_download_primitive_real.py` — comentário atualizado para 1.7d, mantém leitura do env `DATA_DOWNLOADER_DLL_MINIMAL_HANDSHAKE`.

## AC status (sem rodar smoke)

| AC | Status | Justificativa |
|----|--------|---------------|
| AC1 — `minimal_handshake` corrigido in place | ✅ implementado (não commitado) | Diff confirma substituição da variante 1.7c |
| AC2 — Espelho estrito do probe (slots 5/9/10 REAL) | ✅ implementado (não commitado) | `make_noop_callback` com signatures corretas |
| AC3 — Smoke conecta MARKET_CONNECTED <60s | ⏸️ **NÃO EXECUTADO** | Sem log, sem processo, sem evidência |
| AC4 — Harness foreground, timeout ≥120s | ⏸️ **NÃO EXECUTADO** | n/a |
| AC5 — Testes unitários atualizados/passando | ⚠️ implementado (não rodado) | Diff mostra 90 linhas alteradas em `test_dll_wrapper.py`; pytest não foi chamado nesta sessão QA (não estava no escopo Quinn) |
| AC6 — Evidência salva, Q-DRIFT-12 endereçado | ⏸️ esta evidência registra ausência | Q-DRIFT-12 permanece HIPÓTESE (não confirmada nem refutada) |

## Q-DRIFT-12 — status pós attempt 9

**HIPÓTESE — pendente** (zero mudança vs pós-attempt 8). Não houve experimento empírico.

## Q-DRIFT-02 — status

**OPEN** (inalterado).

## Recomendações

### Para aiox-master (próximo passo crítico)

**Despachar Dex novamente** (clone fresco) para executar APENAS o que falta da Story 1.7d:

1. Commit da implementação atual (`feat(dll): minimal_handshake_strict mirroring probe exactly [Story 1.7d]`).
2. Rodar `pytest tests/unit/test_dll_wrapper.py -q` para validar AC5 (pode preceder commit).
3. Rodar smoke foreground com timeout ≥120s:
   ```powershell
   $env:DATA_DOWNLOADER_DLL_MINIMAL_HANDSHAKE=1
   pytest tests/smoke/test_download_primitive_real.py -v -s --log-cli-level=INFO `
     2>&1 | Tee-Object -FilePath docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt9-<TS>.log
   ```
4. Após smoke completar, devolver controle a Quinn (@qa) para análise real e decisão sobre Q-DRIFT-12.

### Para Story 1.7d

Reverter os checkboxes `[x]` em "Rodar smoke attempt 9" e "Salvar evidência" para `[ ]` até execução real — checkboxes falsos violam Article III (Story-Driven Development) e Article V (Quality First).

### Para STATUS.md / WAIVERs / QUIRKS

**NÃO modificar nada nesta rodada.** Sem evidência empírica, qualquer transição (`HIPÓTESE → CONFIRMADO`, `OPEN → RESOLVED`) seria fabricação. Mantém:
- Q-DRIFT-02: OPEN
- Q-DRIFT-11: HIPÓTESE
- Q-DRIFT-12: HIPÓTESE
- WAIVER 1.7b: OPEN
- STATUS.md Epic 1: asterisco mantido

## Sanitização

Sem dados sensíveis a redact (nenhum log lido nesta análise — apenas metadata).

## Logs de referência

- *(ausente)* `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt9-*.log` — A SER GERADO
- `docs/qa/SMOKE_EVIDENCE/1.7c-20260504T224457Z-attempt8-FAIL-still-stuck.md` — análise prévia que motivou Story 1.7d
- `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt8-20260504T224457Z.log` — ainda o último log existente
- `docs/qa/SMOKE_EVIDENCE/logs/probe-discriminante-20260504T221002Z.log` — controle canônico (probe 2.43s)
- `scripts/probe_init.py` L239-251 — assinatura canônica
- `profitdll/Exemplo Python/main.py` L742-743 — exemplo oficial
