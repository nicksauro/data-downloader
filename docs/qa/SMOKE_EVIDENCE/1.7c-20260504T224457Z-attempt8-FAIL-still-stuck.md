# Smoke Evidence — Attempt 8 (Story 1.7c, `minimal_handshake=True`)

**Verdict:** **FAIL-still-stuck** (com caveat crítico — variante NÃO é espelho exato do probe; ver §Análise Técnica)

**Data:** 2026-05-04 22:45:13 BRT (start) → 22:45:25 BRT (última linha do log; processo terminou prematuramente)
**Hash commit:** `2d17923` (`feat(dll): minimal_handshake variant mirroring Nelogica example [Story 1.7c]`)
**Executor:** Quinn (@qa) — análise pós-execução do smoke disparado por Dex
**Hostname:** `<HOME>` (sanitizado)
**Python:** 3.14.3, PySide6 6.11.0, pytest 9.0.2
**Log bruto:** `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt8-20260504T224457Z.log` (20 linhas / 6132 bytes UTF-16)

## Diff exato vs attempt 7

| Item | Attempt 7 (commit `0f76a0f`) | Attempt 8 (commit `2d17923`) |
|------|------------------------------|------------------------------|
| `minimal_handshake` flag | n/a (não existia) | **`True`** (via `DATA_DOWNLOADER_DLL_MINIMAL_HANDSHAKE=1`) |
| `_configure_dll_signatures()` | chamado (25 funções registradas) | **pulado** |
| `SetEnabledLogToDebug(0)` | chamado | **pulado** |
| Slot 3 (`stateCallback`) | REAL (queue) | REAL (queue) |
| Slot 4 (`newTradeCallback`) | NoopCallback | **`None`** |
| Slot 5 (`newDailyCallback`) | NoopCallback | **`None`** ⚠️ (probe passa REAL) |
| Slot 6 (`newHistoryCallback`) | NoopCallback | **`None`** |
| Slot 7 (`priceBookCallback`) | NoopCallback | **`None`** |
| Slot 8 (`offerBookCallback`) | NoopCallback | **`None`** |
| Slot 9 (`progressCallBack`) | NoopCallback | **`None`** ⚠️ (probe passa REAL) |
| Slot 10 (`tinyBookCallBack`) | NoopCallback | **`None`** ⚠️ (probe passa REAL) |
| `os.chdir` (Q-DRIFT-10) | aplicado | aplicado |

Log evento `dll.minimal_handshake_enabled` confirma a configuração (linha 15):
`skipped=['_configure_dll_signatures', 'SetEnabledLogToDebug']`.
Log evento `dll.initialize_call` linha 16: `slots_active=['state'] slots_none=7 total_callback_slots=1 minimal_handshake=True`.

## Linha do tempo (BRT)

| Hora | Evento |
|------|--------|
| 22:45:13 | `dll.companions_check status=ok` |
| 22:45:13 | `dll.cwd_changed` (Q-DRIFT-10 aplicado) |
| 22:45:13 | `dll.loading` |
| 22:45:13 | `dll.minimal_handshake_enabled` (variante ativa) |
| 22:45:13 | `dll.initialize_call slots_none=7` |
| 22:45:14 | `dll.initialized code=0` ✅ |
| 22:45:14 | `dll.market_connect_attempt attempt=1 max_attempts=3 timeout=300` |
| 22:45:15 | `MARKET_LOGIN_OK conn_type=3 result=0` ✅ |
| 22:45:15 | `LOGIN_CONNECTED conn_type=0 result=0` ✅ |
| 22:45:15 → 22:45:25 | 10× `MARKET_DATA/1 conn_type=2 result=1` ❌ (mesmo padrão attempt 7) |
| 22:45:25 | **EOF do log; processo python desapareceu** |

## Métricas

| Métrica | Valor |
|---------|-------|
| Tempo até MARKET_CONNECTED (`result=4`) | **timeout** (jamais atingido) |
| Trades baixados | n/a |
| `MARKET_DATA/1 result=1` consecutivos | 10 (em 10 segundos) |
| Heartbeat `dll.waiting_market_data elapsed_seconds=30` | NÃO disparou (processo morreu antes) |
| Duração efetiva da run | ~12 segundos |
| Pytest summary (PASSED/FAILED/ERROR) | **AUSENTE** — processo killed externamente |

## Análise técnica (Quinn)

### 1. Q-DRIFT-11 — status pós attempt 8: **NÃO confirmado, NÃO refutado**

A premissa mais simples seria: "minimal_handshake espelha o probe; se conectar < 5s → Q-DRIFT-11 confirmado; se travar → Q-DRIFT-11 refutado". **Mas a variante implementada NÃO espelha o probe** — diverge em 3 slots críticos (5/9/10).

**Probe canônico** (`scripts/probe_init.py` L239-251) passa:
- Slot 5: `newDailyCallback` REAL
- Slot 9: `progressCallBack` REAL
- Slot 10: `tinyBookCallBack` REAL

**`minimal_handshake=True` da Story 1.7c** passa `None` em **todos** os 7 slots não-state (incluindo 5/9/10). Documentado nos comentários do próprio wrapper (`wrapper.py:723-734`):

> `slot 5 = newDailyCallback -> None (NoopCallback no probe é REAL — usamos None para o caminho mínimo; se travar aqui, AC3 isolará reintroduzindo REAL one-by-one)`

Portanto: **3 mudanças simultâneas vs attempt 7** (signatures pulados + log nativo não silenciado + 7 slots como `None`), mas também **3 divergências vs probe** (slots 5/9/10 são `None` em vez de REAL).

Conclusões válidas a partir desta evidência:
- ✅ A configuração mais minimalista possível (1 slot ativo + tudo o resto `None`, sem signatures, sem log silence) NÃO conecta em <12s — diferente do probe (1.82s/2.43s).
- ⚠️ Não podemos concluir se foi a ausência de `newDailyCallback`/`progressCallBack`/`tinyBookCallBack` REAIS que travou (nova hipótese), OU se Q-DRIFT-11 está refutado.

### 2. Hipótese emergente — **Q-DRIFT-12 (CANDIDATO):** servidor exige callbacks reais em slots 5/9/10

O exemplo Nelogica (`main.py` L742-743) **e** o probe (L239-251) ambos passam REAL em 5/9/10. Possivelmente o servidor Nelogica condiciona `MARKET_DATA result=4` ao recebimento de uma estrutura/snapshot inicial via daily/progress/tinyBook que requer um callback funcional (não `None`). Se for o caso, `None` em slot 5/9/10 → DLL faz early-return → snapshot não é despachado → handshake não progride.

Esta é uma hipótese **NÃO investigada antes** e merece registro.

### 3. Observação sobre execução prematura

O processo terminou em **12s** (vs 671s do attempt 7). Sem traceback, sem PASSED/FAILED no log, sem heartbeat de 30s. Plausível: harness de background do Dex (provavelmente `run_in_background=true`) timeout/kill curto. Isso significa que **não esperamos os 300s de timeout completos** — em teoria o handshake poderia progredir tardiamente. Improvável (probe conecta em <3s; nenhum precedente em attempts 4-7 mostra progressão tardia), mas tecnicamente não excluído.

### 4. Causa-raiz #2 e #3 — também não isoladas

`SetEnabledLogToDebug(0)` removido + `_configure_dll_signatures()` removido + slots 4/6/7/8 → `None` (mais 5/9/10 → `None`) **simultaneamente** mantém o handshake travado. Não podemos afirmar nada sobre causas #2/#3 individualmente — o experimento não as isola, ele agrega.

## Confirmação ou refutação de Q-DRIFT-11

**PENDING.** A variante testada não é uma bisseção limpa de Q-DRIFT-11; é uma união de Q-DRIFT-11 + outras 4-5 mudanças. AC3 (bisseção reversa one-by-one) é **necessária** para concluir.

## Próxima ação recomendada

**Story 1.7d — espelho EXATO do probe** (não a variante mais minimal possível):

```python
if minimal_handshake_strict:
    # AGORA espelha ESTRITAMENTE main.py L742-743 / probe L239-251
    non_state_slots = [
        None,                    # slot 4  newTradeCallback
        make_noop_callback(...), # slot 5  newDailyCallback  REAL signature
        None,                    # slot 6  newHistoryCallback
        None,                    # slot 7  priceBookCallback
        None,                    # slot 8  offerBookCallback
        make_noop_callback(...), # slot 9  progressCallBack  REAL signature
        make_noop_callback(...), # slot 10 tinyBookCallBack  REAL signature
    ]
```

Se o modo `minimal_handshake_strict` conectar em <5s → confirma hipótese Q-DRIFT-12 emergente (servidor exige callback real em 5/9/10) E refuta Q-DRIFT-11 isolado (porque slots 4/6/7/8 são `None` em ambos).

Se também travar → investigar threading model do `state` callback queue vs sleep/event. O probe usa `time.sleep(0.2)` em loop polling sobre variáveis globais; o wrapper usa `queue.Queue.get(timeout=...)`. Pode haver um deadlock quando o `stateCallback` real (que faz `queue.put_nowait`) interage com a `ConnectorThread` em main.thread bloqueada em `queue.get`.

## Recomendações para STATUS / WAIVER / QUIRKS

- **STATUS.md**: NÃO modificar Epic 1 (smoke ainda bloqueado). NÃO remover asterisco.
- **WAIVER 1.7b** (`docs/qa/WAIVERS/1.7b-real-smoke-deferred-2026-05-04.md`): manter OPEN. Adicionar nota de attempt 8 indicando hipótese Q-DRIFT-11 NÃO confirmada e Q-DRIFT-12 emergente.
- **QUIRKS.md**:
  - Q-DRIFT-02: manter OPEN.
  - Q-DRIFT-11: atualizar status `🧪 HIPÓTESE — pendente confirmação Story 1.7c` → `🧪 HIPÓTESE — attempt 8 NÃO conclusivo (variante divergiu do probe em slots 5/9/10); pendente Story 1.7d com espelho estrito`.
  - Adicionar **Q-DRIFT-12** (novo): hipótese de que servidor Nelogica condiciona `MARKET_DATA → result=4` a receber snapshot inicial via callbacks REAIS em slots 5/9/10.

## Sanitização

- Hostname removido (substituído por `<HOME>`).
- Credenciais não aparecem no log (`credential_redacted=***`, `key_redacted=***`).
- User ID `0433675XXXX` aparece no log linha 16; este relatório não o reproduz.

## Logs de referência

- `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt8-20260504T224457Z.log` (20 linhas — wrapper falha cedo, processo killed)
- `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt7-20260504T215148Z.log` (383 linhas — wrapper falha completa)
- `docs/qa/SMOKE_EVIDENCE/logs/probe-discriminante-20260504T221002Z.log` (probe sucesso 2.43s — controle canônico)
- `scripts/probe_init.py` L239-251 (assinatura canônica REAL nos slots 5/9/10)
- `profitdll/Exemplo Python/main.py` L742-743 (exemplo oficial — REAL nos slots 5/9/10)
