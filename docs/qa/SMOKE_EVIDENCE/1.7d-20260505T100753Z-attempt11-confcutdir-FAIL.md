# Smoke Evidence — Attempt 11 Sub-run 1 (Story 1.7d, --confcutdir bypass)

**Verdict:** **FAIL-still-stuck** — pytest com `--confcutdir=tests/smoke` (bypass de `tests/conftest.py` raiz) **NÃO** destravou. Q-DRIFT-22 **REFUTADA**.

**Data execução:** 2026-05-05 10:07:53 BRT (start) → 10:14:04 BRT (kill por backoff retry attempt=2)
**Executor:** Dex (@dev) — modo autônomo, attempt 11 sub-run 1
**Log bruto:** `docs/qa/SMOKE_EVIDENCE/logs/smoke1-attempt11-confcutdir-20260505T100753Z.log` (45 KB, 213 linhas)

---

## Comando executado

```powershell
$env:PYTHONIOENCODING='utf-8'
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = "1"
$env:PYTHONUNBUFFERED = "1"
$env:DATA_DOWNLOADER_DLL_MINIMAL_HANDSHAKE = "1"

python -m pytest tests/smoke/test_download_primitive_real.py \
  -v -s -p no:cacheprovider --no-header -o addopts="" \
  --confcutdir=tests/smoke
```

`--confcutdir=tests/smoke` instrui o pytest a NÃO subir a árvore além de `tests/smoke/`. Logo, `tests/conftest.py` (raiz, que importa `data_downloader.testing.fixtures` → `MockProfitDLL` → ctypes pre-carregado) **não é executado** nesta sessão.

---

## Resultado bruto

| Métrica | Valor |
|---------|-------|
| Verdict | **FAIL** |
| `dll.initialized code=0` | OK |
| `MARKET_LOGIN_OK` (3,0) | OK |
| `MARKET_DATA/1 result=1` | repetido 2s/2s por 300s |
| `dll.waiting_market_data` heartbeats | 30s, 60s, 90s, ..., 240s |
| `dll.connected_timeout` aos 300s (attempt 1) | sim, ERR_DLL_MARKET_TIMEOUT |
| `dll.market_connect_retry attempt=1 cooldown_seconds=30 max_attempts=3` | sim |
| `dll.market_connect_attempt attempt=2 timeout=300` | iniciou aos 330s |
| Tempo até kill (manual, depois de attempt=2 começar) | 370.6s |

**Sintoma idêntico aos attempts 9 e 10:** callback de state dispara `MARKET_DATA/1 result=1` regularmente, sem promover para `result=4`.

---

## Implicação técnica

A hipótese **Q-DRIFT-22 (Aria)** — que `tests/conftest.py` raiz, ao importar `data_downloader.testing.fixtures`, pré-carrega o módulo `data_downloader.dll.wrapper` em ordem diferente, causando duplo `WinDLL` load ou estado ctypes pré-poluído — **fica REFUTADA empiricamente**.

Mesmo cortando o conftest raiz da árvore via `--confcutdir`, o pytest core (collection, fixture discovery, plugin manager interno, signal handlers, atexit registry, sys.modules residual) **continua suficiente** para o servidor Nelogica não promover `MARKET_DATA` para `(2,4)`.

Isto reforça a conclusão de attempt 10: **o delta entre standalone e pytest está no _core_ do pytest mesmo, não em conftests, plugins externos ou capture/buffering**.

---

## Hipóteses ainda em aberto (post-attempt 11 sub-run 1)

1. **Q-DRIFT-23 (especulativo):** `signal.signal(SIGINT, ...)` de pytest core (instala handler para teardown gracioso). DLL pode chamar `WaitForSingleObject` ou similar dentro da ConnectorThread C que interage mal com signal handlers Python.
2. **Q-DRIFT-24 (especulativo):** `sys.settrace`/`sys.setprofile` residual via assertion rewriting do pytest (mesmo sem pytest-cov).
3. **Q-DRIFT-25 (especulativo):** `atexit` handlers do pytest core que registram cleanup que muda comportamento de threading do MainThread.

Nenhuma destas é diretamente testável sem mais bisection.

---

## Próxima ação

Sub-run 2 (`scripts/run_smoke_real_standalone.py`) — fast-path standalone reproduzindo o **fluxo completo** do test (init → wait_market → download_chunk → asserts). Se PASS, confirma que o bug é exclusivamente do harness pytest e que workaround = rodar smoke como script standalone.

---

## Constitutional notes

- **Article IV (No Invention):** Q-DRIFT-23/24/25 são especulações claramente etiquetadas — não invenção, e não usadas como base para mudanças de produção.
- **Article V (Quality First):** Não consegui PASS, mas ainda assim cada attempt elimina hipóteses concretas. Refutação é evidência válida.
- **Restrições mantidas:** `src/`, `tests/`, `STATUS.md`, WAIVERs, `pyproject.toml` não modificados.
