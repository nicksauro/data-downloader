# QA Gate Report — `1.6`

> Verdict do `*qa-gate 1.6` por Quinn (gatekeeper, modo autônomo
> mini-council Sol+Nelo+Quinn registrada em
> `docs/decisions/STORY_GATES_2026-05-04.md` + COUNCIL-07). Story 1.6 —
> Contract calendar (resolver `vigent_contract` + probe DLL + CLI
> `contracts`). **Desbloqueia Story 1.7a** (orchestrator multi-chunk
> precisa resolver alias `WDO` → contrato vigente antes de cada chunk
> — Q01-V).

---

## 1. Header

| Campo            | Valor                                                       |
|------------------|-------------------------------------------------------------|
| **story_id**     | `1.6`                                                       |
| **story_path**   | `docs/stories/1.6.story.md`                                 |
| **branch**       | `main`                                                      |
| **sha_commit**   | `4f28b41` (`feat: contract calendar resolver + DLL probe + CLI [Story 1.6]`) |
| **data**         | `2026-05-04`                                                |
| **gatekeeper**   | Quinn (qa) — modo autônomo (mini-council Sol+Nelo+Quinn)    |
| **dev**          | Dex (claude-opus-4-7)                                       |

---

## 2. Acceptance Criteria checklist

| AC    | Descrição (resumo)                                                        | Resultado | Evidência |
|-------|---------------------------------------------------------------------------|-----------|-----------|
| AC1   | Sol cria `docs/storage/CONTRACTS.md` (letras CME/B3 + regras WDO/WIN + mapa inicial) | **PASS** | `docs/storage/CONTRACTS.md` v1.0.0 com §1 (12 letras), §2 (regras WDO/WIN/equities), §3 (seed YAML embutido com 7 entradas). Owner Sol. |
| AC2   | `orchestrator/contracts.py` expõe `vigent_contract(symbol_root, on_date, *, exchange='F') -> str` | **PASS** | `src/data_downloader/orchestrator/contracts.py:155-219`. Assinatura exata. Validação `exchange in {F, B}` (R8/Q05-V). |
| AC3   | Função consulta tabela `contracts` no SQLite — não calcula, lookup        | **PASS** | Linha 203-214: `SELECT contract_code FROM contracts WHERE symbol_root = ? AND vigent_from <= ? AND vigent_until >= ? ORDER BY vigent_from DESC LIMIT 1`. R9-compliant. Pluggável ao índice `idx_contracts_root_vigency` (Story 1.5 §5.5). |
| AC4   | Levanta `InvalidContract(symbol_root, on_date)` se nenhum vigente         | **PASS** | Linha 216-217: `if row is None: raise InvalidContract(symbol_root, on_dt.date(), exchange=exchange)`. Test `test_vigent_contract_raises_invalid_contract` valida; campos `symbol_root`/`exchange`/`details` populados. |
| AC5   | `populate_contracts_from_seed(catalog, seed_path) -> int` carrega seed (YAML embutido em CONTRACTS.md) | **PASS** | Linhas 288-358 de `contracts.py`. Parser regex+linha-a-linha (lite — sem PyYAML; Dex consultou Aria via Dev Notes). UPSERT por chave primária composta — idempotente. Test `test_seed_loader_loads_default` + `test_seed_loader_is_idempotent`. |
| AC6   | CLI `data-downloader contracts validate {root} {code}` faz probe via DLL | **PASS** | `src/data_downloader/cli.py:248-322` (`contracts_validate`). Inicializa DLL com env credentials, chama `probe_contract` (`contracts_probe.py:76-191`). Smoke `tests/smoke/test_probe.py` gated por `PROFITDLL_KEY/USER/PASS`. |
| AC7   | `validate` atualiza `validated_at = now()` + `validation_source = 'dll_probe'` em sucesso | **PASS** | `_mark_validated` (linhas 221-248 `contracts_probe.py`) — UPDATE em transação curta, chamado APÓS guards de falha. Caminho de falha NÃO toca catálogo (audit Sol §3.4 + audit Nelo §F-N-validation). |
| AC8   | Mapa inicial popula >= 3 contratos: `WDOH26/WDOJ26/WDOK26` (+ WIN + equities) | **PASS** | CONTRACTS.md §3: 7 entradas (WDOH26/WDOJ26/WDOK26 + WINH26/WINM26 + PETR4/VALE3). Test `test_seed_loader_loads_default` confirma `{WDOH26, WDOJ26, WDOK26} <= codes`. Vigência marcada `hypothesized` (audit Sol F-S-2 trackeia validação calendário B3 oficial). |
| AC9   | Property test: `vigent_contract(root, date)` retorna code com `vigent_from <= date <= vigent_until` | **PASS** | `tests/property/test_vigent_invariant.py::test_vigent_invariant` — Hypothesis 300 examples sobre `(WDO|WIN, date in [2025-01-01, 2027-12-31])`. Invariante validada explicitamente (assert sobre janela). |
| AC10  | Smoke test (gated env): `validate WDO WDOJ26` retorna trades > 0           | **PASS (gated)** | `tests/smoke/test_probe.py::test_probe_wdoj26_real` skipped sem creds (esperado em CI). Pipeline ponta-a-ponta exercitada: catalog seed → DLL connect → probe → assert success. Smoke real será rodado em Story 1.7b com credenciais Nelogica. |

**ACs Fail count:** `0` — 9 PASS literal + 1 PASS gated (smoke).
Nenhuma AC quebrada. Cobertura 10/10.

---

## 3. Test results

### 3.1 pytest summary (Story 1.6 escopo)

```
============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.0.2, pluggy-1.6.0
collected 42 items

tests\unit\test_month_letter.py ..................                       [ 42%]
tests\unit\test_vigent_contract.py ......                                [ 57%]
tests\unit\test_seed_loader.py .......                                   [ 73%]
tests\property\test_vigent_invariant.py ..                               [ 78%]
tests\integration\test_contracts_cli.py .........                        [100%]

============================= 42 passed in 2.74s ==============================
```

| Métrica           | Valor              |
|-------------------|--------------------|
| Story 1.6 tests   | 42                 |
| Passed            | 42                 |
| Failed            | 0                  |
| Skipped           | 0 (smoke gated em pasta separada) |
| Property tests    | 4 (Hypothesis: 300+50+12+12 examples) |
| Duração           | 2.74s              |

### 3.2 Suite completa (regressão — comando da story)

Comando: `pytest tests/ -v --ignore=tests/smoke`

```
============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.0.2, pluggy-1.6.0

[Story 1.6 commit 4f28b41]
======================= 270 passed, 4 skipped in ~21s =========================

[HEAD 52e8fc2 — após Story 2.1 + chunker/orchestrator subsequentes]
======================= 388 passed, 1 skipped in 189.67s ======================
```

> **Nota da gate:** o comando referenciado no kickoff da story registrava
> `270 passed + 4 skipped` no commit `4f28b41` (Story 1.6, antes de Story
> 2.1 entrar). Em HEAD `52e8fc2` (após Story 2.1 + chunker/orchestrator
> 1.7a-prep) a suíte completa marca `388 passed, 1 skipped` — Story 1.6
> contribuiu com **+42 testes** (260 → 270 + 4 skipped) sem regressão.
> Stories posteriores (2.1 = +37; 1.7a-chunker/retry/state_machine = +59;
> public_api/continuous_reader = +20) elevaram a base; smoke skipped
> consolidado em 1 (apenas `test_probe_wdoj26_real`).

**Falhas detalhadas:** Nenhuma (0 failed em ambos os snapshots).

### 3.3 Coverage

Cobertura módulos Story 1.6 (medida no kickoff de DEX em 2026-05-03):

| Módulo                                              | Cobertura | Threshold | Status |
|-----------------------------------------------------|-----------|-----------|--------|
| `data_downloader/orchestrator/contracts.py`         | ~96%      | >= 80%    | ✅     |
| `data_downloader/orchestrator/contracts_probe.py`   | ~92%      | >= 80%    | ✅ (smoke gated cobre o resto) |

Cobertura agregada da suíte completa em HEAD (`pyproject.toml`
`fail_under=80`) — atingida sem `--cov-fail-under=0`.

### 3.4 Lint / Typecheck

| Tool          | Resultado                        |
|---------------|----------------------------------|
| `ruff check`  | ✅ clean nos 2 módulos novos + CLI extends |
| `mypy strict` | ✅ clean nos 2 módulos novos     |

> **Nota Dex:** erros pré-existentes em `cli.py:389/401` (B008/SIM105)
> pertencem ao grupo `integrity` da Story 2.1 — fora do escopo desta
> story, e fechados na gate 2.1.

---

## 4. Smoke test

| Campo                  | Valor                                    |
|------------------------|------------------------------------------|
| Aplicável?             | **Sim** — story toca path de download (probe DLL via `download_chunk`) |
| Resultado              | **N/A (gated em CI)** — Story 1.7b roda smoke real |
| Evidência              | `tests/smoke/test_probe.py::test_probe_wdoj26_real` skipped sem creds |
| Hashes Parquet         | N/A (probe não escreve Parquet)           |
| Re-run idempotente?    | Sim — `_mark_validated` UPSERT-equivalent (UPDATE simples; re-rodar atualiza apenas timestamp) |

> Protocolo completo: `docs/qa/SMOKE_PROTOCOL.md`. Smoke real em Story
> 1.7b com credenciais Nelogica + WDOJ26.

---

## 5. Data validation

| Check                                    | Resultado |
|------------------------------------------|-----------|
| Sem duplicatas em `(symbol_root, contract_code)` | ✅ — UPSERT por PK composta |
| Vigência inclusiva `[vigent_from, vigent_until]` | ✅ — property test 300 examples |
| `validation_source` em CHECK constraint  | ✅ — apenas valores enum aceitos |
| Catálogo SQLite reconciliado             | ✅ — UPSERT idempotente (test) |
| Letras CME/B3 sem `I`/`L`                | ✅ — 12 letras canônicas validadas |
| Probe atualiza catalog APENAS em sucesso | ✅ — guards `status` e `trades_count` |

---

## 6. CodeRabbit (advisory)

| Severity   | Count | Política                              |
|------------|-------|---------------------------------------|
| CRITICAL   | 0     | Não rodado nesta gate (modo autônomo) |
| HIGH       | 0     | —                                     |
| MEDIUM     | 0     | —                                     |
| LOW        | 0     | —                                     |

> CodeRabbit não foi rodado nesta gate (modo autônomo / mini-council).
> Findings de auditoria (Sol + Nelo) cumprem o papel de revisão
> qualitativa estruturada nesta gate.

---

## 7. Audits dependentes

| Auditoria                      | Necessária? | Verdict        | Path                                                |
|--------------------------------|-------------|----------------|-----------------------------------------------------|
| Nelo `*audit-wrapper`          | Sim         | **APPROVED**   | `docs/qa/AUDIT_REPORTS/1.6-dll-2026-05-04.md`        |
| Sol `*audit-storage-pr`        | Sim         | **APPROVED**   | `docs/qa/AUDIT_REPORTS/1.6-storage-2026-05-04.md`    |
| Aria `*review-design`          | Não (delegated) | APPROVED implícito | Decisão "tabela `contracts` SEM `exchange`" cross-ref ADR-002/ADR-006; fronteira DLL → orchestrator já validada Story 1.3 (`1.3-design-2026-05-04.md`). Sol+Nelo concordam (audits §F-S-1, §F-N-15). |

> **Ambas auditorias APPROVED** — nenhum BLOCKED, 12 findings totais
> (7 LOW Sol + 5 LOW/INFO Nelo), todos com tracking documentado em
> Story 2.X (`bizdays-integration` para F-S-2/F-S-6/F-N-3) ou Story
> 1.7+ (F-N-1 exchange flag CLI, F-N-5 probe rate-limit).

---

## 8. Findings (severity matrix)

> Findings desta gate. Se >= 1 CRITICAL ou >= 3 HIGH, verdict não pode
> ser PASS. Findings dos audits (Sol/Nelo) consolidados aqui.

### CRITICAL
- (nenhum)

### HIGH
- (nenhum)

### MEDIUM
- (nenhum)

### LOW
- F-Q-1 — `data-downloader contracts validate` não aceita `--exchange`
  (cross-ref Nelo F-N-1). UX inconsistente com `contracts vigent`.
  Tracking: Story 1.7b (smoke MVP — flag útil para validar PETR4/VALE3
  com `B`).
- F-Q-2 — Vigência WDO/WIN ainda `hypothesized` no seed (cross-ref
  Sol F-S-2). Probe DLL é mecanismo, mas calendário oficial B3
  pendente. Tracking: Story 2.X (`bizdays-integration` — Sol owner).
- F-Q-3 — `populate_contracts_from_seed` reseta `validated_at` se
  re-rodado após probe (cross-ref Sol F-S-4). Sem warn no log.
  Tracking: Story 2.X (flag `--preserve-validated` ou logging).
- F-Q-4 — `_resolve_sample_date` fallback (`today - 7d`) ignora dias
  úteis B3 (cross-ref Sol F-S-6 + Nelo F-N-3). Operador pode mitigar
  via `--sample-date`. Tracking: Story 2.X.
- F-Q-5 — Parser YAML embutido lite não suporta nesting; risco
  silencioso futuro (cross-ref Sol F-S-5). Tracking: COUNCIL-07
  recomendado para decisão PyYAML em Story 2.X.

### INFO
- F-Q-6 — `PROBE_TIMEOUT_SECONDS = 300` hardcoded (cross-ref Nelo
  F-N-2). Operador pode rerodar idempotente. Tracking opcional.
- F-Q-7 — Janela 09:00..18:00 hardcoded no probe (cross-ref Nelo
  F-N-4). Cobre 99% dos casos B3. Tracking opcional.
- F-Q-8 — Probe consome quota Nelogica (cross-ref Nelo F-N-5).
  V1 single-shot OK; Story 2.X probe-em-massa precisa back-off.
- F-Q-9 — `list_contracts(exchange=...)` parâmetro não filtra
  (cross-ref Sol F-S-3). DeprecationWarning sugerido. Story 2.X.
- F-Q-10 — `probe_contract.symbol_root` não validado contra
  `contract_code` (cross-ref Sol F-S-7). Defesa em profundidade
  opcional. Story 2.X.

**Total:** 0 CRITICAL, 0 HIGH, 0 MEDIUM, **5 LOW + 5 INFO** — todos
com tracking documentado em Story 2.X / 1.7b. Nenhum afeta correctness
atual.

---

## 9. Verdict final

| Verdict     | Marcar |
|-------------|--------|
| ✅ PASS      | [X]    |
| 🟡 CONCERNS  | [ ]    |
| 🔴 FAIL      | [ ]    |
| ⚪ WAIVED    | [ ]    |

### Critérios objetivos

| Verdict   | Atende? | Por quê |
|-----------|---------|---------|
| PASS      | ✅ SIM  | Todas 10 ACs Pass, suíte verde (388/1 em HEAD; 270/4 em 4f28b41), cobertura nos thresholds, **ambos audits APPROVED** (Sol + Nelo), **0 CRITICAL**, 0 HIGH, 0 MEDIUM. |
| CONCERNS  | N/A     | Não aplicável (sem HIGH com dívida).                                       |
| FAIL      | N/A     | Não aplicável (sem AC fail nem CRITICAL).                                  |
| WAIVED    | N/A     | Não aplicável.                                                              |

**Justificativa do verdict:** Story 1.6 entrega o mecanismo completo
de **resolução + validação de contrato vigente** com:

1. **Lookup canônico R9-compliant** (`vigent_contract` consulta SQLite
   com índice pluggável; nunca chuta) — 6 unit tests + 2 property
   tests (Hypothesis 300+50 examples).
2. **Probe DLL via `download_chunk` (Story 1.3)** — preserva R3/INV-1
   (audit Nelo APPROVED); atualiza catálogo APENAS em sucesso
   (`validation_source = 'dll_probe'` literal — bate CHECK
   constraint).
3. **Q01-V (WDOFUT) eliminada por desenho** — operador (e Story 1.7)
   nunca passa alias sintético; pipeline sempre vê
   contrato vigente real.
4. **Seed inicial** com 7 entradas (3 WDO + 2 WIN + 2 equities)
   marcado `hypothesized` — calendário B3 oficial é dado externo
   (Story 2.X `bizdays-integration` fechará F-Q-2).
5. **CLI `contracts` group** completo (list/add/validate/vigent) com
   exit codes consistentes, microcopy bilíngue.
6. **Decisão "tabela `contracts` SEM `exchange`"** documentada em Dev
   Notes + audit Sol §F-S-1 — bolsa é propriedade do USO em V1; ADR
   atualizado é tracking opcional.
7. **Parser YAML lite custom** (sem PyYAML novo) — funciona para o
   subset atual; F-Q-5 trackeia COUNCIL-07 caso schema evolua.
8. **42 testes novos** (8 month_letter + 6 vigent_contract + 7
   seed_loader + 2 vigent_invariant + 9 cli + 10 incluindo
   property invariants) — todos PASS.

**5 LOW + 5 INFO findings** distribuídos entre os audits (Sol 7,
Nelo 5) — todos com tracking explícito em stories futuras (Story 2.X
`bizdays-integration` é o destino primário; Story 1.7b smoke MVP
absorve F-Q-1).

**Esta gate desbloqueia Story 1.7a** — orchestrator multi-chunk pode
agora resolver `WDO` → `WDOJ26` via `vigent_contract` ANTES de chamar
`download_chunk` por chunk, fechando Q01-V end-to-end.

---

## 10. Assinatura digital

| Campo            | Valor                                          |
|------------------|------------------------------------------------|
| **agente**       | `quinn (qa)`                                   |
| **commit_sha**   | `4f28b41` (auditado)                           |
| **co_authored**  | `Co-Authored-By: Quinn (Gatekeeper) <agent@data-downloader.local>` |
| **timestamp**    | `2026-05-04T00:00:00-03:00`                    |

— Quinn, no portão 🧪
