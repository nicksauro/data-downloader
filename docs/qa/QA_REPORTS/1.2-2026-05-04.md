# QA Gate Report — Story 1.2

> Quinn `*qa-gate 1.2`. Verdict inegociável.

---

## 1. Header

| Campo            | Valor                                                  |
|------------------|--------------------------------------------------------|
| **story_id**     | 1.2                                                    |
| **story_path**   | `docs/stories/1.2.story.md`                            |
| **branch**       | `main`                                                 |
| **sha_commit**   | `f2a766d`                                              |
| **data**         | 2026-05-04                                             |
| **gatekeeper**   | Quinn (qa)                                             |
| **dev**          | Dex                                                    |

---

## 2. Acceptance Criteria checklist

| AC   | Descrição (resumo)                                                               | Resultado | Evidência                                                                                          |
|------|----------------------------------------------------------------------------------|-----------|----------------------------------------------------------------------------------------------------|
| AC1  | Surface pública `ProfitDLL` (init/wait/finalize/__enter__/__exit__/dll_version)  | Pass      | `wrapper.py:118-513` define todos os métodos; testes contextmgr/version cobrem (`test_dll_wrapper.py:378-446`) |
| AC2  | 11 args fixos no init (3 cred + 1 state + 7 noop), JAMAIS `None`                 | Pass      | `wrapper.py:347-353` + `test_initialize_market_only_passes_11_args_no_none`: `len(args) == 11` E loop `assert a is not None` |
| AC3  | State callback APENAS `put_nowait`                                               | Pass      | `callbacks.py:95-103` body único `state_queue.put_nowait` em `with suppress(Full)`; `test_state_callback_only_calls_put_nowait_via_queue_mock` |
| AC4  | `_cb_refs` global; `finalize` NÃO limpa                                          | Pass      | `callbacks.py:58` global; `wrapper.py:473-475` comentário + `self._dll = None` apenas; `test_finalize_does_not_clear_cb_refs` verifica `len(cb_refs) == initial_count` |
| AC5  | `wait_market_connected(timeout=60)` consome sequência completa, aceita result ∈ {2,4} (Q-AMB-01) | Pass | `wrapper.py:375-424` loop com aliases; `test_wait_market_connected_returns_true_with_canonical_sequence` + `test_wait_market_connected_accepts_market_waiting_result_2` |
| AC6  | `finalize()` tenta `DLLFinalize` → fallback `Finalize` (Q-AMB-02)                | Pass      | `wrapper.py:464-471` try/except AttributeError; `test_finalize_calls_dllfinalize_first` + `test_finalize_falls_back_to_finalize_when_dllfinalize_missing` |
| AC7  | `NL_*` ret < 0 vira `DLLInitError`                                               | Pass      | `wrapper.py:356-367`: `if ret < 0: err = decode_nl_error(ret); raise DLLInitError(...)`; `test_initialize_market_only_raises_dllinit_error_on_negative_return` (NL_INVALID_ARGS) |
| AC8  | Logging structlog com eventos canônicos                                          | Pass      | `wrapper.py` emite 13 eventos canônicos: `dll.companions_check/loading/load_failed/native_log_silenced/native_log_silence_failed/initialize_call/initialized/error/market_state/connected/connected_timeout/finalized/version_resolved/version_unknown/unsupported_platform`. Credenciais mascaradas (`key_redacted/credential_redacted="***"`). NUNCA loga em callback (R3 — verificado em audit Nelo) |
| AC9  | Path resolvido: arg → env `PROFITDLL_PATH` → default                             | Pass      | `wrapper.py:138-160` precedência implementada; `test_path_resolution_explicit_arg_wins/env_var/default_when_no_arg_no_env` (3 testes) |
| AC10 | Smoke gated por `PROFITDLL_KEY`                                                  | Pass (placeholder) | `tests/smoke/test_dll_init.py` com `pytest.mark.skipif(not os.getenv("PROFITDLL_KEY"))`. Smoke real implementado em Story 1.7 (formalização do `SMOKE_PROTOCOL.md`). Aceitável para 1.2 (validado por @po em re-validação 2026-05-03) |
| AC11 | `SetEnabledLogToDebug(0)` ANTES de `DLLInitializeMarketLogin`                    | Pass      | `wrapper.py:307` (try `SetEnabledLogToDebug(0)`) precede `wrapper.py:347` (init); `test_initialize_market_only_calls_set_enabled_log_to_debug_before_init` verifica ordem em `method_calls` |
| AC12 | `verify_dll_companions` ANTES de `WinDLL()`                                      | Pass      | `wrapper.py:267` (`_verify_companions()`) → `wrapper.py:289-292` (lazy import + WinDLL); `_load_verify_dll_companions` carrega script via `importlib.util` (path com hífen). `test_verify_companions_raises_when_path_missing` cobre erro path |
| AC13 | Property `dll_version` cacheada                                                  | Pass      | `wrapper.py:482-513` try `GetDLLVersion`, cacheia, fallback `"unknown"` em AttributeError. 3 testes (`unknown_when_not_initialized`, `cached_after_first_call`, `unknown_on_attribute_error`) |
| AC14 | Fixture `dll_session` session-scoped (Q08-E)                                     | Pass      | `tests/conftest.py:22` `@pytest.fixture(scope="session")`. Placeholder yield None em V1 (init real em 1.7) — documentado |
| AC15 | Test unit `mock_dll.mock_calls == []` no callback (INV-1)                        | Pass      | `tests/unit/test_dll_callbacks.py:78-104` (`test_state_callback_does_not_call_dll_inv1`) — 5 invocações do callback, assert `mock_dll.mock_calls == []`, side-check `real_queue.qsize() == 5` |
| AC16 | `_state_queue = Queue(maxsize=1000)` + política documentada                      | Pass      | `wrapper.py:167` `Queue(maxsize=1000)`. Política `put_nowait` + `suppress(Full)` documentada em `callbacks.py:71-78` (bug-only path; state changes raros). `test_state_callback_swallows_full_silently` valida overflow path |

**ACs Fail count:** 0

---

## 3. Test results

### 3.1 pytest summary

```
106 passed, 2 skipped in 6.09s
```

DLL-only run (Nelo's scope):
```
47 passed, 2 skipped in 1.78s
```

| Métrica           | Valor       |
|-------------------|-------------|
| Total tests (full)| 108         |
| Passed            | 106         |
| Failed            | 0           |
| Skipped           | 2 (1 platform-gated, 1 env-gated AC10) |
| xfail / xpassed   | 0 / 0       |
| Duração (full)    | 6.09s       |
| Duração (DLL only)| 1.78s       |

**Falhas detalhadas:** Nenhuma.

**Skipped:**
- `tests/unit/test_dll_wrapper.py::test_initialize_market_only_raises_on_non_windows` — skipif `sys.platform == "win32"` (gate inverso; só roda em Linux/Mac).
- `tests/smoke/test_dll_init.py::test_dll_init_real` — skipif `not PROFITDLL_KEY` (smoke real precisa de credencial e DLL companions; Story 1.7 formaliza).

### 3.2 Coverage report

Não rodado pytest-cov nesta gate (ADR-014 / Story 1.4 baseline). Cobertura
funcional dos novos módulos `dll/` cobre todos os paths críticos via 47
testes unit dedicados. Cobertura formal de threshold `>= 70%` em `dll/`
deferida para Story 1.4.5 (Pyro baselines + cov consolidado pós-1.4).

| Módulo                       | Status                                       |
|------------------------------|----------------------------------------------|
| `data_downloader/dll/`       | 47 testes dedicados; paths críticos cobertos |
| `data_downloader/public_api/exceptions.py` | 6 testes em `test_dll_errors.py` cobrem DLLInitError |

### 3.3 Lint / Typecheck

| Tool                     | Resultado                  |
|--------------------------|----------------------------|
| `ruff check`             | All checks passed!         |
| `mypy --strict`          | Success: no issues found in 7 source files |

Comando reproduzível:
```
python -m ruff check src/data_downloader/dll/ src/data_downloader/public_api/ tests/unit/test_dll_*.py tests/smoke/test_dll_init.py tests/conftest.py
python -m mypy src/data_downloader/dll/ src/data_downloader/public_api/
```

---

## 4. Smoke test

| Campo                  | Valor                                              |
|------------------------|----------------------------------------------------|
| Aplicável?             | Parcial — smoke real planejado para Story 1.7      |
| Resultado              | N/A (placeholder gated por `PROFITDLL_KEY`)        |
| Evidência              | `tests/smoke/test_dll_init.py` (skipif gate funciona) |
| Hashes Parquet         | N/A (story 1.2 não escreve Parquet)                |
| Re-run idempotente?    | N/A para 1.2; init→finalize→init é PROIBIDO (Q08-E / M15) — fixture session-scoped enforça |

> Justificativa: AC10 explicitamente declara smoke como "gated"; @po validou
> em 2026-05-03 (`STORY_VALIDATIONS_2026-05-03.md` 10/10). Smoke real (E2E
> contra DLL ao vivo) é gate de Epic 1 (Story 1.7) — `SMOKE_PROTOCOL.md`
> formaliza ali.

---

## 5. Data validation

N/A — Story 1.2 não produz Parquet.

---

## 6. CodeRabbit (advisory)

Não rodado nesta gate (config WSL pendente). Nelo audit + Aria review +
checklist Quinn cobrem o escopo. CodeRabbit habilitado como advisory após
Story 1.5 (CI integration).

---

## 7. Audits dependentes

| Auditoria                      | Necessária? | Verdict   | Path                                                |
|--------------------------------|-------------|-----------|-----------------------------------------------------|
| Nelo `*audit-wrapper`          | Sim         | APPROVED  | `docs/qa/AUDIT_REPORTS/1.2-dll-2026-05-04.md`       |
| Sol `*audit-storage-pr`        | Não         | N/A       | Story 1.2 não toca `storage/`                       |
| Aria `*review-design`          | Sim         | APPROVED  | `docs/qa/AUDIT_REPORTS/1.2-design-2026-05-04.md`    |

---

## 8. Findings (severity matrix)

### CRITICAL
Nenhum.

### HIGH
Nenhum.

### MEDIUM
Nenhum.

### LOW

- **F-L-1:** Wording impreciso "11 callback slots" em comentários (Nelo F-1) — story narrativa diz "11 slots", chamada real tem 11 args = 3 cred + 8 callbacks. Sem impacto operacional. Tracking: ajustar em revisão de story 1.3+.
- **F-L-2:** Reuso de `MARKET_WAITING=2` na constante usada para alias `(ROTEAMENTO, 2)` é semântica menor (Nelo F-2). Considerar adicionar `ROTEAMENTO_CONNECTED: Final[int] = 2` em 1.3.
- **F-L-3:** Q09-AMB / Q-AMB-02 ainda `ambiguous` — wrapper já loga `method_used` para fechar via smoke real (Nelo F-3). Tracking: Story 1.7 atualiza QUIRKS.md `ambiguous` → `validated`.
- **F-L-4:** Cobertura formal `pytest-cov` não medida nesta gate (consolidar em Story 1.4.5 com baseline Pyro). 47 testes dedicados cobrem paths críticos.
- **F-L-5:** Smoke E2E real (`test_dll_init_real`) é placeholder gated por env (AC10) — implementação completa em Story 1.7 (`SMOKE_PROTOCOL.md`).

---

## 9. Verdict final

| Verdict     | Marcar |
|-------------|--------|
| ✅ PASS      |  [x]   |
| 🟡 CONCERNS  |  [ ]   |
| 🔴 FAIL      |  [ ]   |
| ⚪ WAIVED    |  [ ]   |

### Critérios objetivos aplicados

PASS = Todas ACs Pass (16/16) + suíte verde (106/108 + 2 skipped esperados)
+ audits APPROVED (Nelo + Aria) + 0 CRITICAL + 0 HIGH + 0 MEDIUM. Cobertura
formal deferred-by-scope (F-L-4) com tracking — não bloqueante.

**Justificativa:** Wrapper ProfitDLL implementado fielmente ao manual. Lei
R3 / INV-1 verificadas EM TESTE (`mock_dll.mock_calls == []`). 11 args sem
`None` verificados EM TESTE (`len(args) == 11` + loop). Q-AMB-01, Q-AMB-02,
Q11-E todos endereçados. ruff PASS, mypy strict PASS, detect-secrets PASS.
Aria validou design (thread model, fronteiras, error propagation). Nelo
validou wrapper (signatures, hot path, finalize). 5 findings LOW — todos
com tracking documentado em stories futuras (1.3, 1.4.5, 1.7).

**Próximo passo desbloqueado:** Story 1.2 → Done. **Wave 5** liberada:
Story 1.3 (Dex + Nelo: history callbacks via `SetHistoryTradeCallbackV2` +
`TranslateTrade`) consome o wrapper em estado conectado.

---

## 10. Assinatura digital

| Campo            | Valor                                                  |
|------------------|--------------------------------------------------------|
| **agente**       | quinn (qa)                                             |
| **commit_sha**   | `f2a766d` (código auditado; deste relatório no commit subsequente) |
| **co_authored**  | `Co-Authored-By: Quinn (Gatekeeper) <agent@data-downloader.local>` |
| **timestamp**    | 2026-05-04T00:00:00-03:00                              |

— Quinn, no portão 🧪
