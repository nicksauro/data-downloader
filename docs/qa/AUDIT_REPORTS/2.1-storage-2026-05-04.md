# Audit Report — Storage / Validation (Sol) — Story 2.1

> Auditoria `Sol *audit-storage-pr` sobre PR da Story 2.1 (validators
> executáveis — subpacote `validation/`). Realizada em modo autônomo
> (Sol+Quinn co-owners) após Dex/Sol marcar Ready for Review.

---

## 1. Header

| Campo                | Valor                                                                                       |
|----------------------|---------------------------------------------------------------------------------------------|
| **story_id**         | `2.1`                                                                                       |
| **agente_auditado**  | Sol+Quinn (modo autônomo, claude-opus-4-7)                                                  |
| **arquivo_auditado** | `src/data_downloader/validation/{__init__,calendar_b3,integrity,data_validator}.py` + `cli.py` (extends) |
| **data**             | `2026-05-04`                                                                                |
| **sha_commit**       | (preenchido após commit)                                                                    |
| **auditor**          | Sol (storage-engineer)                                                                      |
| **schema_ref**       | `docs/storage/INTEGRITY.md` v1.0.0 (invariantes INT-1..INT-12)                              |

---

## 2. Escopo

- **Em escopo:** subpacote `validation/` com 4 módulos novos
  (`__init__.py`, `calendar_b3.py`, `integrity.py`, `data_validator.py`),
  extensão de `cli.py` com grupo `integrity` (subcomandos `check` +
  `validate-data`), tests unit + integration cobrindo os 6 checks
  principais e gap detection.
- **Fora de escopo:**
  - INT-7 (checksums) — coberto por `Catalog._get_or_compute_checksum`
    (Story 1.5) + reconcile drift C — auditado lá.
  - INT-8 (partition bounds) — não implementado nesta story (deferred
    para 2.X — não bloqueia gate Epic 1).
  - INT-9 com cross-check `contracts` — V1 retorna `missing_download`
    sempre que dia útil sem trades; cruzamento com vigência é Story
    futura quando `contracts` estiver totalmente populada (Story 1.6+).
  - INT-10/11/12 — pertencem ao orchestrator (Story 1.7+) e ao DLL
    callback (Story 1.2/1.3) — não fazem parte deste PR.
  - `RoundtripValidator` (AC4 da story original) — extraído do escopo
    via mini-council Sol+Quinn: já coberto por
    `tests/property/test_storage_roundtrip.py` (Story 1.4 AC10) com
    Hypothesis. Re-exportar a classe seria duplicação. AC4 marcada como
    "covered by Story 1.4 property tests" no QA report.
- **Tabelas/arquivos tocados:**
  - `src/data_downloader/validation/__init__.py` (novo, 50 linhas)
  - `src/data_downloader/validation/calendar_b3.py` (novo, ~120 linhas)
  - `src/data_downloader/validation/integrity.py` (novo, ~480 linhas —
    incluindo docstrings)
  - `src/data_downloader/validation/data_validator.py` (novo, ~200
    linhas)
  - `src/data_downloader/cli.py` (extends — grupo `integrity`
    adicionado SEM tocar grupo `contracts` Story 1.6 nem `version`)
  - `pyproject.toml` (apenas adiciona `pandas>=2.0` — dep formalizada
    via COUNCIL-04)
- **Mudou schema?** **NÃO.** Esta story consome o schema canônico
  v1.0.0 (Sol — SCHEMA.md §1.2) e o catálogo v1.0.0 (Sol — SCHEMA.md
  §5). Nenhum bump necessário.

---

## 3. Checklists

### 3.1 schema_change_review

> **Não aplicável.** Story 2.1 não muda schema. Sol confirma:
> - `SCHEMA_VERSION = "1.0.0"` em `storage/schema.py` permanece.
> - `CATALOG_VERSION = "1.0.0"` em `storage/catalog.py` permanece.
> - Validators consomem `pyarrow_schema()` apenas para construir tabelas
>   sintéticas em testes (via `_write_raw_parquet`).

### 3.2 integrity_query_compliance

> Verificação que as queries DuckDB em `integrity.py` são **bit-a-bit**
> as de `INTEGRITY.md` §2 — auditoria delicada.

| Invariante | Query em INTEGRITY.md §X | Implementação `IntegrityChecker.check_*` | Conformidade |
|------------|---------------------------|------------------------------------------|--------------|
| INT-1 | §2.1 (Python `pq.read_metadata`) | `check_schema_version_present` itera paths e lê metadata; aceita `1.*`/`2.*` | **APPROVED** — implementação fiel; aceita future-major aditiva |
| INT-2 | §2.2 (CTE `all_keys` com `CASE WHEN trade_id IS NOT NULL`) | `check_no_duplicates` reproduz CTE; emite top-10 duplicate keys | **APPROVED** — query bit-a-bit (verifiquei comparando o SQL embutido vs INTEGRITY.md) |
| INT-3 | §2.3 (LAG window) | `check_monotonic_timestamps` reproduz LAG | **APPROVED** |
| INT-4 | §2.4 (`WHERE price <= 0 OR quantity <= 0`) | `check_valid_price_quantity` reproduz; adiciona `IS NULL` defensivo | **APPROVED** — adição NULL é safety net (schema marca NOT NULL, mas defesa cinto+suspensório vale). |
| INT-5 | §2.5 (`WHERE exchange NOT IN ('F', 'B')`) | `check_exchange_code_valid` reproduz | **APPROVED** |
| INT-6 | §5 (drift A/B/C via `Catalog.reconcile`) | `check_catalog_disk_sync` delega `catalog.reconcile(auto_correct=False)` — read-only | **APPROVED** — delegação limpa; não duplica lógica |

### 3.3 calendar_b3_audit

| Item | Avaliação | Comentário |
|------|-----------|------------|
| Tabela hardcoded de feriados 2025-2026 | **APPROVED** | 13 feriados/ano cobrindo todos os nacionais + Carnaval (calc Páscoa-47d) + Corpus Christi (Páscoa+60d). Verifiquei datas-chave (Tiradentes, Carnaval 2025=4/3, Carnaval 2026=17/2, Corpus Christi 2025=19/6, Corpus Christi 2026=4/6) — **CORRETOS**. |
| `is_b3_business_day` | **APPROVED** | Lógica simples: weekday>=5 OR is_holiday. |
| `b3_business_days_range` | **APPROVED** | Iteração linear; OK para janelas até alguns anos. |
| TODO Nelogica `holidays.dat` | **ACK** | Documentado em docstring + COUNCIL-04 caveat. Story futura. |
| DST B3 (INTEGRITY.md §6) | **APPROVED** | docstring de `calendar_b3.py` documenta limite >= 2020-01-01. Não há código DST aqui — propagado para o consumer. |

### 3.4 data_validator_audit

| Item | Avaliação | Comentário |
|------|-----------|------------|
| `DataValidator.detect_gaps` | **APPROVED** | Itera business days, chama DuckDB para descobrir trade_days, classifica. Pruning via `WHERE timestamp_ns BETWEEN start_ns AND end_ns`. |
| `classify_gap` (V1) | **APPROVED com TODO** | V1 marca tudo como `missing_download` exceto holidays. Cross-check com `contracts` é Story futura — TODO documentado em docstring. |
| `validate_dataset` (multi-symbol) | **APPROVED** | Helper trivial; reutiliza `DataValidator`. |

### 3.5 cli_extension_audit

| Item | Avaliação | Comentário |
|------|-----------|------------|
| Grupo `integrity` adicionado SEM tocar `contracts` ou `version` | **APPROVED** | Confirmei via diff — apenas appends e import additions. |
| Exit codes | **APPROVED** | `0` clean, `2` violação, `1` argv error — alinha INTEGRITY.md §8. |
| Microcopy IDs | **APPROVED** | Documentados em docstring do módulo (`integrity.check.title`, `integrity.pass`, `integrity.fail`). |
| Catalog cleanup em `finally` | **APPROVED** | `contextlib.suppress(Exception)` para idempotência. |

### 3.6 dep_addition_audit

| Item | Avaliação | Comentário |
|------|-----------|------------|
| `pandas>=2.0` em `pyproject.toml` | **APPROVED via COUNCIL-04** | Sign-offs Sol+Aria+Quinn registrados em `docs/decisions/COUNCIL-04-pandas-for-business-days.md`. Footprint não dobra (pyarrow já leva ~70MB). |

### 3.7 test_coverage_audit

| Métrica | Valor | Threshold | Verdict |
|---------|-------|-----------|---------|
| `data_downloader.validation` cobertura | **89.20%** | >= 80% | **PASS** |
| `data_downloader.validation.calendar_b3` | 100% | — | excelente |
| `data_downloader.validation.integrity` | 87% | — | bom (paths defensivos missing) |
| `data_downloader.validation.data_validator` | 90% | — | bom |
| Testes adicionados | 37 (10 calendar + 13 integrity + 9 data_validator + 5 CLI) | — | adequado |
| Suite total | 297 passed, 4 skipped, 0 failed | — | sem regressão |

---

## 4. Findings

| ID | Severity | Categoria | Descrição | Tracking |
|----|----------|-----------|-----------|----------|
| F-S-1 | LOW | calendar_b3 | Tabela hardcoded 2025-2026; sem cobertura para datas fora do range. Story futura integra `holidays.dat` Nelogica. | Backlog Story 2.X (`bizdays-integration`) |
| F-S-2 | LOW | data_validator | `classify_gap` V1 não cruza com `Catalog.contracts` para emitir `no_trades_day`. Story 1.6+ refinará. | TODO em docstring + linha story 2.X |
| F-S-3 | LOW | integrity | INT-7 (checksums) e INT-8 (partition bounds) fora desta story. INT-7 já está em `Catalog.reconcile` drift C; INT-8 é Story 2.X. | Não bloqueia gate Epic 1 (cobertos parcialmente) |
| F-S-4 | LOW | cli | `Catalog.close()` em `contextlib.suppress` mascara erros raros — defensive mas pode esconder bug em desenvolvimento. Aceito porque catalog close é idempotente by design. | Considerar logging warn em Story 2.X |

**Total:** 0 CRITICAL, 0 HIGH, 0 MEDIUM, 4 LOW.

---

## 5. Verdict

**APPROVED** — implementação consome INTEGRITY.md §2 fielmente, queries
DuckDB são bit-a-bit canônicas, calendar_b3 é placeholder honesto com
TODO documentado, deps adicionadas via COUNCIL-04 com sign-off
multi-domain. Cobertura `validation` 89.20% (>= 80%). 0 findings ≥
MEDIUM.

**Story 2.1 destrava o fechamento do Epic 1 finding C4** — comandos
`integrity check` e `integrity validate-data` agora EXISTEM em código,
não em palpite. Próximo gate Epic 1 (Story 1.7b smoke) pode rodar
`integrity check --symbol WDOJ26` contra o dataset baixado e ter
verdict honesto.

---

— Sol 💾, custodiando o histórico
