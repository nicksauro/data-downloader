"""data_downloader.ui.widgets — Widgets customizados reutilizáveis (Epic 3).

Owner: Felix (frontend-dev) | Design: Uma (ux-design-expert).

**Status:** Epic 3 — TODO (placeholders skeleton, COUNCIL-12 prep).

Widgets compostos consumidos pelas screens:

    - ``symbol_picker.py``  — Story 3.2 — autocomplete de contratos vigentes.
    - ``period_picker.py``  — Story 3.2 — dropdown presets + custom range.
    - ``progress_card.py``  — Story 3.2 — barra + subtitle + log + cancel.

Widgets futuros (não criados ainda — Felix expande conforme necessidade
durante Epic 3):

    - ``toast.py``        — notificações flutuantes (Story 3.2/3.3).
    - ``file_dialog.py``  — wrapper QFileDialog com DontUseNativeDialog (Story 3.4).
    - ``log_view.py``     — log streaming expansível (Story 3.2).

Referências:
    - docs/ux/WIREFRAMES.md (componentes em cada tela)
    - docs/ux/QT_PATTERNS.md (padrões Qt)
    - docs/decisions/COUNCIL-12-epic3-prep.md
"""

from __future__ import annotations

__all__: list[str] = []
